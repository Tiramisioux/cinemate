# C6 — application record, 2026-08-26

Fixes applied the same day as the review, per the SONNET-PROMPT contract (every item
re-verified against the then-current dev tips before editing).

| Repo | Branch | Base | Commits |
|---|---|---|---|
| cinemate | `docs/c6-correctness-pass` | dev `54f29dfa` | `ce5c54c4` c6.1 dead keys · `5ac16653` c6.2 stale behaviour · `638f5d3b` c6.3 README · `c45d7556` c6.4 mkdocs/orphans · `d3774b2b` c6.5 installation-steps mechanical · `b006d16d` c6.7 home/overview |
| cinepi-raw | `docs/c6-readme-pass` | dev `bd39389` | `1328ff4` c6.6 README restructure |

Both branches local, not pushed (operator approval required). Worktrees:
`development/worktrees/c6-docs/` and `c6-cinepi/`.

**Verification run:** `mkdocs build --strict` green on the c6 branch (remaining INFOs:
brick/coverpage/todo deliberately nav-hidden; one pre-existing `#enabling-i²c` self-anchor
in installation-steps' new content). Every fixed settings key re-grepped against
`src/module/` — `system.storage.auto_preroll`, `audio_capture`, `follow_preview`/
`always_both`, `pip_cam0` all resolve in code. cinepi-raw README: fences balanced, all
flagged defects grepped gone.

## Outcomes vs DOCS-FIX-PLAN.md

**Applied (cinemate):** auto_preroll key ×3 pages · `audio_capture` ×2 · record_policy
values + `hdmi_display.preview.*` paths · README dead branch link · sensors.md CCMP12
row/footnote + H1 + neutral mode-set caveat · quad "ships enabled" ×2 + stock sample
mapping · format-button location · config-txt canonical block (+rp1-overclock, +cec) ·
k_steps stock value (settings-json) · controller-methods pip values · redis-keys command
name · cli-user-guide flags table → canonical pointer · changelog (imx585 copy-paste,
stray `**`, ClearHDR + settings-editor entries, 5-state cycle) · web-api unverified note ·
web-gui link → simple-gui · speed-ramping truncation + pin 18→16 · recovery-console dup
paragraph · README reshape (title/features/first-run/"works out of the box") · docs
acknowledgments +PiShrink · mkdocs (4 orphans + 2 js deleted, cinepi-multi into nav,
extra_css fix, Material-only theme config removed, Contributing→About) · todo trim ·
installation-steps mechanical (gren ×2, shuld, password wording, stray nano line) ·
index/overview bit-depth claim + imx585_mono parity.

**Applied (cinepi-raw):** dep order before the libcamera build · redundant chmod/ninja
block dropped · working `meson setup build` invocation · cpp-mjpeg-streamer → upstream
nadjieb (what the installer builds) · cg_rb 3.5,1.5 · flags table +`--zoom`
+`--scaler-crops` +`--max-pixel-rate`, `--hdr` values documented · new 12-bit ClearHDR
(CCMP12) section, 16-bit split out as the quality path · WAV-offset section: dead
"clock-correction flag" reference removed, real key path named · asound.conf path/heading/
two-card block · structure (one H1, Tests/CI + License at bottom) · typos.

**Already fixed upstream (no change needed):** `docs/b13-5-readme-fix` merged into
cinepi-raw dev before this ran (python3-pip + Tests/CI) · quad dial numbering
(hardware-controls table corrected on dev today) · run_cinemate.sh creation step (added in
today's installation-steps update) · overclocking.md manual-patch staleness (page rewritten
today around `--max-pixel-rate`) · `--same-hdmi` description.

**Deferred, with reasons:**
- sensors.md IMX283 mode-set reconciliation — needs a `--list-cameras` capture from the Pi
  and a sensors.json decision; the page now states the mismatch neutrally.
- installation-steps content fixes beyond mechanical (k_steps para, I²C-note rationale,
  dep-line dedup, "12 bit system" line) — operator rework of that page in flight on
  `feature/dev-track`; reconcile there.
- compile-raw.sh single-sourcing and installer-block dedup across README/Overview/
  installation-steps — installer/structure changes, out of scope for a docs pass.
- `libtiff5-dev` package-name validity on Bookworm — needs a Pi check; kept as-is in both
  repos (matches the installer's own package list).
- 503 body-string mismatch (web-api "err busy" vs building-control-units "err too many
  clients") — two different 503 sources; verify against api.py before touching.
