# System
<!-- sidebar group `system` · tab: settings.jsonc -->

Edit the headings and the paragraphs. Leave the `<!-- key: ... -->` lines alone —
they are what the GUI looks each string up by when CineMate starts.

---

## Restart and log output
<!-- key: pane.system -->

Apply saved changes, and watch what CineMate is doing — the console below is a live tail of its log.

### (help text not attached to a card)
<!-- key: help.system.0 -->

Restarting applies any saved changes above and takes about 10 seconds. Recording stops if one is in progress. The console below reports when the camera answers again.

### (other text in this pane)
<!-- key: text.system.0 -->

A full reboot for any other reason.

### (note box)
<!-- key: note.system.0 -->

Every field on this page is one key in `settings.jsonc` on the Pi. Open the drawer any time to see exactly what Save would write — nothing here is a separate database.
