# Playback
<!-- sidebar group `playback` · tab: Playback -->

Edit the headings and the paragraphs. Leave the `<!-- key: ... -->` lines alone —
they are what the GUI looks each string up by when CineMate starts.

---

## Playback
<!-- key: pane.playback -->

_(no pane description)_

### (note box)
<!-- key: note.playback.0 -->

**Achieved rate is measured, not assumed.** The player holds the clock and skips frames the camera could not decode in time, and shows the count — rather than letting the take quietly run slow.

### Use conform frame rate
<!-- key: card.playback.0 -->

Plays every take on the project's conform rate, from `settings.conform_frame_rate`. A take shot faster than the conform rate plays as slow motion — the same speed it will run on the timeline. Off plays each take at the rate it was shot.
