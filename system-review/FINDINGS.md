# FINDINGS — append-only index

One line per finding. Detail files live in `findings/F-###.md` where the finding needs
more than three lines. Schema and severity/confidence definitions: `CONVENTIONS.md` §5.1.

**Do not renumber. Do not reuse IDs. Append only.**

| id | severity | confidence | repo | category | summary | evidence |
|---|---|---|---|---|---|---|
| F-001 | medium | confirmed | cinemate | dead-code | **Corrected by PI-001** (was: "not copied by installer"; four templates). Deployed to every camera by the installer — the deletion has real effect, not just repo hygiene. Five unreferenced HTML templates, not four: `template_old.html` (13213 B) was missed by the original count. PR #132 already deletes all five, so the PR is fine and the finding was not | `src/module/templates/` (incl. `template_old.html`), `src/module/app/template.html`; PI-001 |
| F-002 | high | confirmed | cinemate | standards | `requirements.txt` lists stdlib `wave` as a package; duplicates `sugarpie`/`flask_socketio`; lists both `Pillow` and `pillow`; mixes docs-build deps into runtime deps | `requirements.txt:1-22` |
| F-003 | high | confirmed | install | install-drift | `requirements.txt` is never referenced by the installer; the two dep lists have diverged by 11+4 packages; `flask` is never installed directly. **Hardware-confirmed by PI-004**: a clean install succeeds end to end (after two real installer bugs found and fixed, F-279/F-280) and reaches a working, recording camera; `flask` confirmed transitive-only via `Flask-SocketIO`, settling the open question directly | `cinemate-install.sh:922-927`, `requirements.txt`; PI-004 |
| F-004 | medium | confirmed | docs | docs-drift | Five 0-byte docs + two 1-line stubs; six commented-out nav lines; 15 of 50 docs unreachable from nav — incl. `image-circle.md` (159 LOC of real content) | `mkdocs.yml:23,57-62`, `deliverables/CENSUS.md` §9 |
| F-005 | high | confirmed | cinemate | standards | No Python lint/format/type config anywhere; cinepi-raw has `.clang-format`, cinemate has nothing | repo root |
| F-006 | high | confirmed | cinemate | test-gap | No CI runs on PRs or on `dev` at all — the sole workflow triggers only on push to `main`, and development happens on `dev`. 27 pytest files never run. **Hardware-confirmed by PI-002**: the full suite (381 passed + 241 subtests, zero skips/errors) passes cleanly on real hardware, matching the off-hardware baseline exactly — B4's CI gate can trust a zero-failure bar | `.github/workflows/docs.yml:9-13`, PR #129 (0 checks); PI-002 |
| F-007 | high | confirmed | cinemate | gui | Colour constants hand-duplicated between `simple_gui.py` Python tuples and `template.html` CSS custom properties, synced only by comment | `simple_gui.py:21-45`, `app/templates/template.html:23-40` |
| F-008 | high | confirmed | cinemate | gui | HDMI GUI lays out from 1920-reference absolute pixel constants, proportionally scaled — not adaptive layout | `simple_gui.py:27-45`, `simple_gui.py:1657-1658` |
| F-009 | low | confirmed | cinemate | structure | `_test/` mixes 27 pytest files with 3 non-test utilities and 4 underscore-prefixed probable-dead files | `_test/` |
| F-010 | medium | confirmed | both | structure | Nine Python modules and four C++ sources exceed 850 LOC; five exceed 1300 | `deliverables/CENSUS.md` §2 |
| F-011 | — | refuted | cinemate | — | **Not reproducible.** KICKOFF §6.4 claimed 8 uncommitted files; working tree is clean and `origin/dev == 02b5a39 == branch base`. See `findings/F-011.md` | `git status` |
| F-012 | low | confirmed | cinepi-raw | dead-code | `cinepi/_mjpegPreviewStage.cpp` (240 LOC) is not in the meson source list and is included by nothing | `cinepi/meson.build:24-34` |
| F-013 | medium | confirmed | cinemate | dead-code | `src/stream.py` is a dead second Flask entry point with a broken import and a wrong-arity `create_app` call | `src/stream.py:2`, `src/module/app/__init__.py:6` |
| F-014 | medium | confirmed | docs | docs-drift | 18 of 84 `ParameterKey` members are absent from `docs/redis-keys.md`; every documented key does exist in code | `redis_controller.py:18-113`, `docs/redis-keys.md` |
| F-015 | medium | confirmed | cinemate | standards | `ParameterKey` is convention, not enforcement — `set_value` accepts any string; 3 live keys bypass the enum, one in SCREAMING_CASE | `redis_controller.py:235`, `ssd_monitor.py:44` |
| F-016 | medium | confirmed | both | redundancy | Redis key `audio_vu` is hand-duplicated across the repo boundary — same constant name and value declared independently in C++ and Python. **PI-006 confirmed the VU meter works end to end** (rendered correctly on the physical HDMI display and in Redis during a real take) **and downgrades the severity**: a deliberately-deleted key produced no visible degradation, because `cinepi_sound` republishes `audio_vu` unconditionally on essentially every sub-second cycle — a stale/missing read is never observable in practice. The duplication itself remains a maintenance risk, just not a silent-failure one | `cinepi_sound.cpp:22`, `simple_gui.py:21`; PI-006 |
| F-017 | medium | confirmed | cinemate | dead-code | `src/module/timekeeper.py` (243 LOC) is entirely dead — `Timekeeper(` appears nowhere in the repo; `main.py` pins `timekeeper = None` | `main.py:658,1026-1027`, `timekeeper.py` |
| F-018 | low | confirmed | cinemate | dead-code | `handle_vu_output()` (main.py:633-644) is dead — its only call site is commented out; contains the commented-out write that makes `vu_meter` a phantom key | `main.py:633-644,743` |
| F-019 | medium | confirmed | cinemate | dead-code | Two write-only Redis keys: `FSCK_STATUS` (3 writes, sole reader commented out) and `user_changing_fps` (sole reader is dead `timekeeper.py`) | `ssd_monitor.py:260,804,816`, `simple_gui.py:2123`, `timekeeper.py:224` |
| F-020 | low | confirmed | cinemate | structure | `simple_gui` reaches through `redis_controller.r` to the raw redis client, bypassing `get_value()` and its cache — a fourth Redis access pattern | `simple_gui.py:1166-1172` |
| F-021 | low | confirmed | cinemate | standards | Bare `set` on its own line in the boot path — a no-op expression statement, the only one in `src/`. Any linter catches this instantly (see F-005) | `main.py:685` |
| F-022 | medium | confirmed | cinemate | dead-code | `RedisController.stop_listener()` is never called; the pub/sub `_listen` thread started in `__init__` has no shutdown path | `redis_controller.py:188,410` |
| F-023 | high | confirmed | cinemate | correctness | `cleanup()` never calls `usb_monitor.stop()` or `ssd_monitor.stop()` although both exist; `redis_listener` (2084 LOC) and `storage_preroll` define no `stop()` at all | `main.py:954-1046`, `usb_monitor.py:528`, `ssd_monitor.py:152` |
| F-024 | low | confirmed | cinemate | structure | The Flask/SocketIO `stream` thread is the only ad-hoc thread in `main.py` not marked `daemon=True`, and cleanup neither stops nor joins it | `main.py:935`, cf. `main.py:467,806` |
| F-025 | high | confirmed | cinemate | correctness | **Upgraded by PI-007/F-285** (was: probable, medium). Only text commands are serialised — CLI/serial/HTTP share `_dispatch_lock`, but GPIO, pots and the quad rotary call controller methods directly with no lock (keyboard also would, but is dead — F-031). Hardware proof (F-285): a live Grove Base HAT pot on `iso`, continuously turned, produced a **100% starve** — 20/20 explicit `set iso` calls over 14s never landed as the live value, out-polled roughly every 130ms by the pot thread with no error surfaced anywhere. Not an occasional race; the default outcome whenever an analog input is connected and moving. See F-268/F-269 for the fuller structural picture (3 serialised paths, 6 bypassing modules, no internal fallback) | `cli_commands.py:21,218,257`, `gpio_input.py:53`, `analog_controls.py:128`, `quad_rotary_controller.py:114`; PI-007, F-285 |
| F-026 | medium | confirmed | cinemate | correctness | Controller method names are a user-facing API resolved by `getattr` from `settings.jsonc`; a typo yields a silently dead button — failure is a log line, not a visible error | `quad_rotary_controller.py:114-123`, `gpio_input.py:38-49`, `settings.jsonc:256-283` |
| F-027 | high | confirmed | both | correctness | **Reclassified by PI-008** (was category `redundancy`, framed as dead code). The static claim still holds exactly: 12 Redis key strings cinepi-raw handles have zero references in cinemate. The inference that they were dead does not: PI-008's full boot→record→resolution-change→shutdown trace showed all 11 distinct concerns are live. Two groups — 8 keys (`awb`, `compress`, `shutter_s`, `thumbnail`, `thumbnail_size`, `pll_kp`, `pll_ki`, `pll_deadband_us`) form an undocumented launch-config contract cinepi-raw reads from Redis on every (re)start with no source-tracked writer (values persist via RDB snapshot from an unknown seed); 2 keys (`pll_phase_err_us`, `pll_req_dur_us`) are per-frame phase-lock telemetry, SET ~1401 times each in 60s (~every frame at 25fps) with zero reader. An undocumented live contract is worse than dead code | `cinepi_state.hpp:23-52`, `cinepi_controller.cpp:354-577`, `redis_controller.py:18-113`; PI-008 |
| F-028 | medium | confirmed | both | standards | The cross-repo key contract has two independent registries with different mechanisms and no shared source: 84-member Python `Enum` vs 24 C++ `#define`s | `redis_controller.py:18`, `cinepi_state.hpp:23-52` |
| F-029 | medium | confirmed | cinepi-raw | dead-code | `cinepi/lj92.c` + `lj92.h` (1218 LOC) are entirely dead — in no meson source list, included by nothing but itself, zero callers of `lj92_encode`/`open`/`decode`. Largest dead file found in the review | `cinepi/lj92.c`, `cinepi/meson.build:24-34` |
| F-030 | medium | confirmed | both | test-gap | cinepi-raw has a working `meson test` target (`phase_lock_core_test`) wired into the build; cinemate's 27 pytest files have no runner at all. The pattern cinemate needs already exists next door | `cinepi/meson.build:62-67`, cf. F-006 |
| F-031 | medium | confirmed | cinemate | dead-code | `src/module/keyboard.py` is dead — class `Keyboard` is never instantiated and `module.keyboard` is never imported. The keyboard control surface does not exist at runtime | `src/module/keyboard.py:5`, no callers repo-wide |
| F-032 | high | confirmed | install | install-drift | 7 of the 11 installer-only packages have zero Python importers: `sysv_ipc`, `keyboard`, `watchdog`, `inotify_simple`, `rpi_hardware_pwm`, `luma.oled`, `pigpio-encoder`. OLED uses `adafruit_ssd1306`, PWM uses `lgpio` | `cinemate-install.sh:922-927`, `i2c_oled.py:5`, `gpio_output.py:25` |
| F-033 | low | confirmed | cinepi-raw | correctness | `CinePISound` stops its child via `pkill -f "cinepi-audio-capture.*--discard-output"` — a cmdline pattern match that would kill any matching process, not just its own child | `cinepi_sound.cpp:619` |
| F-200 | low | confirmed | cinepi-raw | dead-code | `cinepi_manager.cpp` is a 0-byte file; `cinepi_manager.hpp` (37 LOC) is referenced by nothing. An interface that was never implemented | `cinepi/cinepi_manager.cpp`, `cinepi/cinepi_manager.hpp` |
| F-201 | medium | confirmed | cinepi-raw | dead-code | `add-redis-timecode.patch` and `add-tc.patch` are byte-identical in their hunks and only partially applied (15 of 41 substantial added lines in tree) — neither applied nor pending | repo root, `cinepi/dng_encoder.{cpp,hpp}` |
| F-202 | high | confirmed | both | redundancy | `tc_cam0`/`tc_cam1` are written by BOTH repos from different inputs by different algorithms — last write wins on a value the operator reads mid-take; docs name only one writer | `cinepi_controller.cpp:334`, `redis_listener.py:909,914`, `docs/redis-keys.md:59` |
| F-250 | medium | confirmed | both | redundancy | The cinepi-raw `--mode W:H:B:{P\|U}` string grammar is built independently at 3 Python sites and parsed by `sscanf` in C++ | `cinepi_controller.py:1465`, `cinepi_multi.py:467,787`, `cinepi-raw/core/options.cpp:55` |
| F-251 | high | confirmed | cinemate | redundancy | **Already drifted.** Config defaults declared in 4 independent registries — `setdefault` calls, JSON Schema `default`, a shipped JSONC template, and the live config — and **11 keys disagree** | `config_loader.py:149-410`, `settings.schema.json`, `resources/settings/settings_default.jsonc`, `settings.jsonc` |
| F-252 | medium | confirmed | cinemate | redundancy | `system.web_api.*` and `system.recovery.*` defaults are stated 3× each and live outside the central loader entirely | `settings.schema.json:38-56`, `web_api_settings.py:10-25`, `cinemate-recovery.py:78-84` |
| F-253 | high | confirmed | both | redundancy | **Already drifted.** "SMPTE base = round(fps)" implemented at 4 sites with **3 different rounding rules** — Python uses banker's rounding, C++ half-away-from-zero and half-up, so they disagree at half-integer fps. **Field impact measured by PI-010:** at 24.5 fps the DNG encoder's own SMPTE frame field wraps at base 24 (Python's banker's-rounding convention), not the base-25 a C++ half-up rule would produce — the prediction guessed the wrong direction, but the divergence itself is real; the Redis-side base was not cross-checked with the same rigor this round | `redis_controller.py:334`, `simple_gui.py:794`, `cinepi_sound.cpp:154`, `dng_encoder.cpp:1178`; PI-010 |
| F-254 | medium | confirmed | cinemate | redundancy | The truth-set `("1","true","yes","on")` is re-typed in 7 files across 8 helpers; two disagree with the other five on unknown input (return the default vs `False`) | `config_loader.py:10`, `simple_gui.py:50`, `mediator.py:80`, `cinepi_controller.py:345`, +4 |
| F-255 | medium | confirmed | cinemate | redundancy | One Redis boolean is re-decoded in 6 places under 3 incompatible rules — some accept `"true"/"yes"/"on"`, some only integers, one only the exact string `"1"` | `redis_controller.py:218`, `redis_listener.py:557`, `ssd_monitor.py:1181`, `simple_gui.py:937,1707`, `cinepi_controller.py:1235` |
| F-256 | high | confirmed | cinemate | redundancy | **Already drifted.** `arrays.iso.steps` / `arrays.shutter_a.steps` are stated 7× across 4 languages (Python, JSONC, HTML attrs, HTML text, JS) and the shutter table is **missing `346.6` in exactly one** | `config_loader.py:246`, `settings.jsonc:138`, `settings_default.jsonc:64`, `settings_editor.py:72`, `settings_editor.html:1302-1313,3268` |
| F-257 | low | confirmed | cinemate | redundancy | `populate_values()` publishes `iso_label`/`fps_label`/… for the web GUI, but `template.html` hardcodes the same strings and never reads the published channel | `simple_gui.py:789-833`, `template.html:429-491` |
| F-258 | low | confirmed | cinemate | redundancy | The `{ext4, exfat, ntfs}` set is stated 3× — a Python guard, a JS display map, and a JS dropdown | `ssd_monitor.py:997`, `template.html:643`, `settings_editor.html:3301` |
| F-259 | medium | confirmed | both | redundancy | **Already drifted.** "Redis `iso` ÷ 100 = libcamera `AnalogueGain`" is stated twice in cinepi-raw, nowhere in cinemate, and the cold-start fallback skips the division. **Field impact confirmed by PI-011**, via a genuine standalone cold start bypassing cinemate's Python layer entirely: the fallback clamps analogue gain to the sensor's maximum (22.26x, ~5.6x over the "iso 400" it silently writes back) with zero error surfaced anywhere. But PI-011's own first attempt showed cinemate's Python `CinePiController` normally re-seeds `iso` into Redis before cinepi-raw needs it on a standard `cinemate-autostart` restart — the bug is real and confirmed, but likely unreachable through normal operation; it would bite a standalone cinepi-raw launch (development, debugging, or a future integration that skips cinemate's Python layer) | `cinepi-raw/cinepi/cinepi_controller.cpp:74,403,76-77`; PI-011 |
| F-260 | low | confirmed | cinemate | redundancy | `"/home/pi/cinemate/settings.jsonc"` is re-typed in 7 files, and the in-code comment that indexes those copies lists only 4 — one with a wrong line number | `settings_editor.py:45-48`, `main.py:51`, `wifi_hotspot.py:44`, `cinepi_controller.py:27`, +3 |
| F-261 | medium | confirmed | cinemate | redundancy | **Already drifted.** `FALLBACK_PACKING_INFO` restates `sensors.json` packing values but omits `packing_by_platform`, so a missing database silently un-packs imx477/imx296 on Pi 4 | `sensor_detect.py:12-19,647-654`, `resources/sensors.json:7-8,40-41` |
| F-100 | medium | confirmed | cinemate | dead-code | `rotary_encoder.py` / `SimpleRotaryEncoder` never imported; superseded by `gpio_input.RotaryEncoder` | `src/module/rotary_encoder.py:4` |
| F-101 | medium | confirmed | cinemate | redundancy | 5 `.pyc` files committed to git despite `.gitignore`; one is for a deleted `adc` module | `src/module/__pycache__/adc.cpython-39.pyc` |
| F-102 | medium | confirmed | cinemate | dead-code | 6 of 13 `Mediator` methods have no subscriber and no caller | `src/module/mediator.py:34` |
| F-103 | low | confirmed | cinemate | dead-code | `Mediator.usb_monitor` and `.redis_listener` assigned but never used | `src/module/mediator.py:11` |
| F-104 | medium | confirmed | cinemate | dead-code | `hasattr(self.stream, "toggle_background_color")` can never be true — unreachable branch | `src/module/mediator.py:151` |
| F-105 | medium | confirmed | cinemate | redundancy | Four independent `StrictRedis` clients with hardcoded `localhost:6379`, bypassing `RedisController` | `src/module/usb_monitor.py:141,439,458,581` |
| F-106 | medium | confirmed | cinemate | redundancy | `CAPTURE_GAIN_REDIS_KEY` re-declares an existing `ParameterKey`; `"is_recording"` used as a bare literal | `src/module/usb_monitor.py:14` |
| F-107 | medium | confirmed | both | redundancy | **Corrected & upgraded by coordinator.** The five `MIC_*` keys are published by **both repos independently** — same key names and the same debug string — and read by neither | `usb_monitor.py:441-447,459`, `cinepi_sound.cpp:1783-1789` |
| F-108 | high | confirmed | cinemate | dead-code | `SSDMonitor.stop()` has no caller **and** would raise `AttributeError` on `self._jthread` if it did — fixing either half alone makes it worse | `src/module/ssd_monitor.py:155` |
| F-109 | medium | confirmed | cinemate | dead-code | 70-line `_journal_loop` unreachable; sole reference is a commented-out thread target. Makes `python3-systemd` an unused install dep | `src/module/ssd_monitor.py:1254`, `cinemate-install.sh:523` |
| F-110 | — | duplicate | cinemate | — | **Merged into F-017.** Same fact (`timekeeper = None`, guard can never fire) | see F-017 |
| F-111 | low | confirmed | cinemate | readability | At least 8 multi-line commented-out code blocks, two of them file-tail dumps | `src/module/framebuffer.py:173` |
| F-112 | medium | confirmed | cinemate | correctness | **Corrected by coordinator (was high).** The `if self.ssd_monitor.is_mounted:` guard is commented out so `unmount_drive()` now runs unconditionally. The claim that it calls a non-existent `mount_cfe` is **wrong** — that whole else-branch is commented out too | `src/module/cinepi_controller.py:2032-2040` |
| F-113 | — | duplicate | cinemate | — | **Merged into F-019** (FSCK_STATUS write-only). Agent 1's remediation differs and is preferred: *add* `FSCK_STATUS` to `ParameterKey` and wire a reader, rather than delete | see F-019 |
| F-114 | medium | confirmed | cinemate | redundancy | Superseded `arecord -vvv` VU path still present alongside the live Redis `audio_vu` path | `src/module/usb_monitor.py:467` |
| F-115 | medium | confirmed | cinemate | dead-code | Entire `USBDriveMonitor` class unreferenced outside its own definition | `src/module/usb_monitor.py:33` |
| F-116 | medium | confirmed | cinemate | redundancy | 6 wrapper accessors around `get_resolution_info` never called; `get_packing` is silently wrong on Pi 4 (cf. F-261) | `src/module/sensor_detect.py:616` |
| F-117 | high | confirmed | cinemate | redundancy | The 46-entry `ACTION_METHODS` catalogue is duplicated verbatim in Python and JS, and `GET /api/actions` — the only thing validating it — has zero consumers | `src/module/app/settings_editor.py:63` |
| F-118 | high | confirmed | cinemate | correctness | That duplication already shipped a bug: catalogue offers `set_log`, the real method is `set_log_encode`, so the settings-editor button silently no-ops | `src/module/app/settings_editor.py:94` |
| F-119 | medium | confirmed | cinemate | dead-code | 7 controller methods with no static caller, no `settings.jsonc` binding and no catalogue entry | `src/module/cinepi_controller.py:768` |
| F-120 | low | confirmed | cinemate | dead-code | 5 `SSDMonitor` properties and `get_mount_status` have no reader | `src/module/ssd_monitor.py:166` |
| F-121 | low | confirmed | cinemate | dead-code | `read_dmesg_log`, `handle_file_change`, `reset_undervoltage_flag` have no caller | `src/module/dmesg_monitor.py:29` |
| F-122 | medium | confirmed | cinemate | dead-code | **Corrected reachability result:** exactly 4 of 48 modules are unreachable from `main.py` (376 LOC, 352 already known). The unreferenced-module avenue in `src/` is exhausted | `src/module/rotary_encoder.py:1` |
| F-123 | low | confirmed | cinemate | redundancy | 3 self-declared compatibility aliases with zero callers, each shadowing a live function | `src/module/config_loader.py:143` |
| F-124 | medium | confirmed | cinemate | redundancy | Two implementations of "is the hotspot up?"; the `main.py` one is dead and uses a different mechanism | `src/main.py:529` |
| F-125 | low | confirmed | cinemate | dead-code | 7 further functions with no caller anywhere in the repo | `src/module/gpio_input.py:146` |
| F-126 | high | confirmed | cinemate | correctness | Four divergent `_as_bool` implementations for the same settings values — `_as_bool(2)` is `True` in `gpio_input` and `False` in the other three | `src/module/gpio_input.py:163` |
| F-127 | medium | confirmed | cinemate | redundancy | Four separate `class Event` definitions with divergent error handling and signatures | `src/module/usb_monitor.py:17` |
| F-128 | medium | confirmed | cinemate | structure | 43 functions in `src/` exceed 60 lines; 8 exceed 250, topped by `run_application` (405), `analyze_frames` (401), `populate_values` (369), `_build_args` (337) | `deliverables/READABILITY-REPORT.md` §1 |
| F-129 | medium | confirmed | cinemate | readability | Nesting reaches **depth 11** in `_start_monitoring` and depth 10 in `listen_stats`; 86 sites across 20 functions sit at depth ≥5 | `dmesg_monitor.py:101`, `redis_listener.py:1064` |
| F-130 | high | confirmed | cinemate | correctness | 337 `except` handlers in ~19,800 LOC (one per 59 lines); **15 swallow silently** with a bare `pass`/`continue` — a direct violation of the project's own "fail visible, never silent" principle | `cli_commands.py:332`, `redis_listener.py:552,1184,1557`, `serial_handler.py:66,117`, +9 |
| F-131 | low | confirmed | cinemate | standards | Two bare `except:` clauses, which also catch `KeyboardInterrupt` and `SystemExit` | `cli_commands.py:159,167` |
| F-132 | medium | confirmed | cinemate | readability | Docstring coverage on public defs is **27%** (152 of 543); **40 public classes have none**, including `CinePiController` whose 94 method names are a user-facing API (F-026) | `cinepi_controller.py:40`, +39 |
| F-133 | — | confirmed | cinemate | readability | **PRESERVE — load-bearing comment inventory.** 47 comments encode *why*, including falsified experiments and a cross-repo invariant. Not a defect: a deletion hazard. See report §4 | `storage_profiles.py:41-49`, `ssd_monitor.py:1122-1125`, `sensor_detect.py:745-752` |
| F-134 | — | confirmed | cinemate | readability | **Positive finding.** Zero `TODO`/`FIXME`/`XXX`/`HACK` markers in the entire Python codebase — unusual discipline at ~19,800 LOC | `src/` |
| F-150 | medium | confirmed | cinemate | dead-code | `_test/_wifi_hotspot_service.py` is a superseded standalone hotspot daemon with hardcoded creds, contradicting the live credential ladder | `_test/_wifi_hotspot_service.py:6,33` |
| F-151 | medium | confirmed | cinemate | redundancy | `_test/_gpio_output.py` (190 LOC) and `__gpio_output.py` (16 LOC) are stale forks of the live `gpio_output.py`; 346 diff lines | `_test/_gpio_output.py:93`, `src/module/gpio_output.py` |
| F-152 | medium | confirmed | cinemate | redundancy | `_test/_mediator.py` is a stale fork of `src/module/mediator.py`, diverged by 112 diff lines | `_test/_mediator.py:7`, `src/module/mediator.py:7` |
| F-153 | low | confirmed | cinemate | dead-code | `_test/automount.py` is a vestigial I2C-button automount unrelated to `services/storage-automount/`; zero inbound refs | `_test/automount.py:1-6,120` |
| F-154 | low | confirmed | cinemate | dead-code | `_test/analyze_logs.py` hardcodes a log path and is never imported or invoked | `_test/analyze_logs.py:129` |
| F-155 | high | confirmed | cinemate | redundancy | `YANK_ERRNOS` is defined **byte-identically twice** — in the automount service and in the app — with no shared module | `services/storage-automount/storage-automount.py:76-82`, `src/module/ssd_monitor.py:46-52` |
| F-156 | high | confirmed | cinemate | redundancy | **Already drifted.** The filesystem→mount-options table is duplicated across the two processes that both mount `/media/RAW`, and **the two copies disagree** | `ssd_monitor.py:599-604`, `storage-automount.py:140-144,322-328` |
| F-157 | medium | confirmed | cinemate | redundancy | The ext4 option string `rw,noatime,nodiratime,commit=60` is stated 5× — once in `ssd_monitor`, 4× in the automount `PROFILES` table | `ssd_monitor.py:45`, `storage-automount.py:89,98,107,120` |
| F-158 | medium | confirmed | cinemate | redundancy | Two independent device-classification implementations with **incompatible taxonomies** (`cfe_nvme/usb_nvme/usb_ssd/nvme_hat/other` vs `SSD/CFE/NVMe/Unknown`) | `storage-automount.py:217-243`, `ssd_monitor.py:463-500` |
| F-159 | medium | confirmed | cinemate | redundancy | Partition→root-block-device derivation duplicated — a helper in the service, inline in the app | `storage-automount.py:173-178`, `ssd_monitor.py:474-481` |
| F-160 | high | probable | cinemate | structure | **Two processes independently mount, fsck and unmount `/media/RAW` with no lock or ownership protocol between them** — the recording target | `storage-automount.py:393,492,363`, `ssd_monitor.py:639,784,527` |
| F-161 | high | confirmed | cinemate | dead-code | `services/cinemate-services.Makefile` recurses into `ssd-automount`, `nvme-automount`, `cfe-hat-automount` — **all three directories were deleted**; zero inbound refs, cannot run | `services/cinemate-services.Makefile:4-6,9-11` |
| F-162 | low | confirmed | cinemate | dead-code | `services/Makefile` declares `uninstall` targets `.PHONY` but generates no recipe — `make -C services uninstall` silently does nothing | `services/Makefile:6,15,28,36-44` |
| F-163 | medium | confirmed | install | install-drift | `python3-systemd` is reachable only through the dead `_journal_loop` — confirms the F-109 corollary; F-032's unused-package list becomes **8 of 11** | `cinemate-install.sh:523`, `ssd_monitor.py:14,139-144,1254` |
| F-164 | medium | confirmed | cinemate | dead-code | **Root cause of F-155..F-160:** the intended service↔app coupling is a dead `journalctl -fu storage-automount` tail, so the app re-implements mount detection by polling instead | `ssd_monitor.py:139-144,1316` |
| F-165 | medium | confirmed | cinemate | dead-code | Root `CMakeLists.txt` does `add_subdirectory(src/module/audio_sync)` — that directory does not exist, so `cmake .` fails immediately | `CMakeLists.txt:4` |
| F-166 | high | confirmed | cinemate | standards | **`settings.schema.json` cannot reject an unknown key** — `"additionalProperties": true` appears 25 times and `false` zero times, including at the document root; a misspelled setting validates clean and is then silently ignored at read time | `settings.schema.json:60,63,78,…,392` (25 sites), `settings.jsonc:2` (`$schema` declared) |
| F-167 | medium | confirmed | cinemate | standards | The two most structurally complex settings blocks are declared with **no properties at all** — `quad_rotary_controller.encoders` is `{"type":"object"}` and `rotary_encoders` is `{"type":"array"}`; their leaves are reflectively-dispatched method names, so a typo is a silent no-op (same failure mode as F-118) | `settings.schema.json:300,276` |
| F-168 | medium | confirmed | cinemate | standards | **Two logging idioms coexist**: 615 module-level `logging.<level>(` calls across 26 files vs 112 named-logger calls across 7 files. No file uses both consistently and nothing selects between them | `src/**` (grep counts); named-logger files: `parameters.py:24`, `app/api.py:20`, `app/raw_files.py:26`, `app/settings_editor.py:36`, `status_broadcast.py:15`, `wifi_hotspot.py:30`, `gpio_input.py:15` |
| F-169 | medium | confirmed | cinemate | standards | **26 statement-level `print()` calls survive.** `battery_monitor.py` accounts for 18 of them and contains zero logging calls — its entire output bypasses the file handler, the colour formatter and the in-app log queue | `battery_monitor.py` (18), `cinepi_controller.py` (3), `grove_base_hat_adc.py` (3), `main.py:573` + 1 |
| F-170 | low | confirmed | cinemate | standards | Mixed log interpolation — 233 f-string calls vs 161 lazy `%`-arg calls. The f-string form formats even when the level is disabled, which matters for the `logging.debug` calls on the frame path | `src/**` (grep counts) |
| F-171 | medium | confirmed | cinemate | dead-code | **`configure_logging(MODULES_OUTPUT_TO_SERIAL, level)` never references its first parameter.** `MODULES_OUTPUT_TO_SERIAL = ['cinepi_controller']` therefore configures nothing while reading as live per-module serial routing | `logger.py:77-109`, `main.py:50,581` |
| F-172 | high | confirmed | cinemate | correctness | **The in-app log queue is never drained.** `QueueHandler.emit` puts every record into an unbounded `queue.Queue()`; the only reader peeks under the mutex (`list(log_queue.queue)[-limit:]`) and never `get()`s. Unbounded growth for the life of the process — a camera that runs for days. **Hardware-confirmed by PI-013**: RSS growth is monotonic in both phases and ~70x faster while recording (~60 KB/s) than idle (~0.85 KB/s), directly matching the prediction | `logger.py:7-13,104-109`, `main.py:226-230`; no `log_queue.get` anywhere in `src/`; PI-013 |
| F-173 | low | confirmed | cinemate | standards | `configure_logging` hardcodes `/home/pi/cinemate/src/logs` — same hardcoded-absolute-path family as F-260, and it puts runtime state inside the source tree | `logger.py:96` |
| F-174 | low | confirmed | install | strength | **STRENGTH — the shell is the cleanest code in the repo.** `shellcheck` over all 11 scripts: 15 findings total, of which the 1916-line `cinemate-install.sh` contributes exactly one. Five scripts are clean. Shell hygiene here already exceeds the Python | `shellcheck -f gcc` over `**/*.sh`; `cinemate-install.sh:111` is the sole hit |
| F-175 | low | confirmed | cinemate | correctness | `cinemate-update.sh` uses `echo "\n----- … -----"` — bash `echo` without `-e` emits a literal `\n`, so the intended blank line never appears | `cinemate-update.sh:15,68` |
| F-176 | medium | confirmed | cinemate | correctness | **`local x=$(git …)` masks git's exit status** (SC2155) at 4 sites; `cinemate-update.sh:25,26` capture `git rev-parse` this way, so a failed rev-parse yields an empty string that then compares unequal and is read as "an update is available" | `cinemate-update.sh:21,25,26`, `cinemate-install.sh:111` |
| F-177 | low | probable | install | correctness | `/etc/profile.d/cinemate-startup-failure.sh` uses `[[ ]]` twice with no shebang or `# shellcheck shell=` directive. `/etc/profile.d/*.sh` is sourced by POSIX login shells too; under `dash` `[[` is a syntax error. Unverified whether any Pi login path uses a non-bash shell | `services/cinemate-autostart/cinemate-startup-failure.sh:3,8`, installed as `PROFILE_HOOK_PATH` at `Makefile:12` |
| F-178 | low | confirmed | cinemate | dead-code | `scripts/pi_cinemate_cli.sh:6-8` assigns `INPUT_FILE`, `LOG_FILE`, `PID_FILE` and never reads any of them | `scripts/pi_cinemate_cli.sh:6,7,8` |
| F-179 | low | confirmed | install | dead-code | `camera-ready.sh` captures `exit_code=$?` from `cinepi-raw --list-cameras` and never reads it — the readiness decision is made purely by grepping stdout, so a non-zero exit is indistinguishable from "no camera" | `services/cinemate-autostart/camera-ready.sh:110,112` |
| F-180 | low | confirmed | cinemate | duplication | `pwm_pin`'s default of `19` is stated twice — in the schema and in code. Currently in agreement; nothing checks that it stays so. Duplicate truth caught *before* it drifted, unlike the other 10 | `settings.schema.json` (`hardware_outputs.pwm_pin.default`), `config_loader.py:393` |
| F-181 | low | confirmed | cinemate | strength | **STRENGTH — PRESERVE.** `wifi_hotspot.py` is the only module with a correct standalone entry point: `basicConfig` guarded behind `if __name__ == "__main__"`, so importing it never hijacks the root logger. This is the pattern the other 32 modules should copy | `wifi_hotspot.py:749-750` |
| F-182 | low | confirmed | install | correctness | **Downgraded by PI-012** (was: "load-bearing for boot", high). `INSTALL_ALT_GPIO_BACKEND=0` genuinely skips both of the installer's own lgpio-install steps — that structural claim still holds — but on current Raspberry Pi OS Bookworm, `python3-lgpio` ships via apt as a `python3-gpiozero`/`python3-rpi-lgpio` dependency independent of the flag, so `main.py`'s unguarded `import lgpio` never fails and the predicted crash does not reproduce. Now interesting only as: do the installer's two redundant lgpio-install code paths ever diverge from the apt-provided one | `cinemate-install.sh:36,324,895,929-931,910`; chain `main.py:21` → `gpio_output.py:4` → `rpi_gpio_wrapper.py:1`; PI-012 |
| F-183 | — | — | cinemate | duplication | **MERGED into F-003 — coordinator correction.** S01 already computed the requirements.txt / installer divergence; `STATE.md` says not to re-derive it and S06 did anyway. The one thing to carry across: S06's set-based count (pip-normalised, deduplicated) is 12 shared / 11 installer-only / **7** requirements-only, against F-003's "11+4". These **agree** — the extra 3 are the docs-build packages F-002 counts separately. Also newly confirmed: `requirements` has zero inbound references anywhere in the repo, not just in the installer | see `findings/F-003.md` |
| F-184 | — | — | cinemate | duplication | **MERGED into F-002.** The stdlib-`wave` observation is S01's. S06 adds only the consumer citation: `simple_gui.py:4` imports the stdlib module, so the requirement line can never have been load-bearing | see F-002 |
| F-185 | — | — | cinemate | duplication | **MERGED into F-002.** The duplicate lines are S01's finding. S06 adds the exact figure: 22 lines, 19 distinct after pip name normalisation | see F-002 |
| F-186 | — | — | install | duplication | **MERGED into F-003.** "`flask` is never installed directly" is S01's. S06 adds the consumer list (`app/__init__.py:2`, `app/api.py:15`, `app/settings_editor.py:18`, `app/main/routes.py:1`) and the `probable` mechanism — it arrives transitively via `flask_socketio`, unverified offline. **Hardware-confirmed by PI-004**: `pip show flask` on a clean install reads `Required-by: Flask-SocketIO` — no longer probable | see `findings/F-003.md`; PI-004 |
| F-187 | low | confirmed | install | dead-code | `sounddevice` is pip-installed on every camera and referenced **nowhere** in the repository | `cinemate-install.sh:926`; zero hits for `sounddevice` in `**/*.py` |
| F-188 | low | confirmed | cinemate | dead-code | `pyaudio` is in `requirements.txt` and imported nowhere. It is also the entry that most often breaks a `pip install -r` (needs portaudio headers) | `requirements.txt:5`; zero `import pyaudio` in `src/`, `_test/` |
| F-189 | — | — | cinemate | duplication | **MERGED into F-002.** Docs-build packages in the runtime requirements set is S01's finding | see F-002 |
| F-190 | low | confirmed | cinemate | standards | **Nothing is pinned.** 20 of 22 `requirements.txt` lines carry no version constraint; the installer's 23-package pip list carries none at all. A rebuild of the same tag resolves to different versions | `requirements.txt`, `cinemate-install.sh:922-927` |
| F-191 | low | confirmed | install | duplication | `cinemate-install.sh` reimplements `config_loader.strip_jsonc()` in a heredoc with an explicit **"Keep in sync by hand"** comment — the fourth hand-sync comment found in this review. Unlike the other three it *states its reason* (the heredoc runs under system `python3`, outside the venv), which makes it the model the standards rule should generalise: duplication is allowed when the reason is named — but it still has no check | `cinemate-install.sh:1633-1636`, `config_loader.py:518-525` |
| F-192 | — | confirmed | install | strength | **PRESERVE — installer idempotency is designed, and the design is written down.** A `# >>> cinemate-install >>>` / `<<<` managed-block pair that is deleted-then-rewritten rather than appended; `ensure_repo` branching on `-d "$dir/.git"`; `[[ -L \|\| ! -e ]]` before replacing the symlink; a `grep -q` guard before the libcamera source patch with the reasoning in a comment ("a re-run cannot leave it half-applied"). Re-running the installer is safe **by construction**, and it is the only part of the repo that argues for its own correctness this way | `cinemate-install.sh:112-113,1540-1550,984`, `ensure_repo`, `:394-402`, `:750-757` |
| F-193 | low | confirmed | install | correctness | The one gap in that design: the Pi 5 overclock patch to `libcamera`'s `minPixelProcessingTime` is applied only if `grep -q '... / 380'` matches, and there is **no `else` branch**. If upstream libcamera changes that expression the patch becomes a silent no-op and the build proceeds without it — the overclock companion fix is simply absent, with no warning. A `warn` in the else costs one line | `cinemate-install.sh:753-757` |
| F-194 | — | confirmed | install | strength | `set -Eeuo pipefail` appears three times: the installer itself, and **both** scripts it generates at install time (`compile-raw.sh`, `run_cinemate.sh`). Strict-mode discipline is propagated into generated artefacts, which is more than most hand-written shell manages | `cinemate-install.sh:3,798,1483` |
| F-195 | low | confirmed | install | test-gap | Corollary to F-174: the repo-wide `shellcheck` sweep covers 11 scripts, but the two scripts the installer *writes* (`~/compile-raw.sh`, `~/run_cinemate.sh`) exist only after an install and are therefore never linted by anything. They are short, but they are the two scripts a user actually invokes | `cinemate-install.sh:796-798,1481-1484` |
| F-203 | high | confirmed | cinemate | gui | **The web GUI has no state model of its own — it consumes the HDMI GUI's.** `handle_connect` does `initial_values.update(simple_gui.populate_values())`, and every subsequent update is a delta of that same dict. The operator's hypothesis ("all three renderings derive from `simple_gui`") is **half true and the half matters**: the *state* is already unified; only the *presentation* is duplicated. Reframes ADR-001 — option C's hard part is already built | `app/main/events.py:57`, `simple_gui.py:705-1073`, `app/main/routes.py:29-32` (the comment says so outright) |
| F-204 | high | confirmed | cinemate | correctness | **One raising subscriber permanently kills the entire live-state bus, silently.** `Event.emit` is a bare synchronous `for fn in list(self._handlers): fn(data)` with no try/except; `_listen`'s `for msg in self.ps.listen():` body has no exception handling either; the thread is `daemon=True` with no `is_alive()` check, no watchdog and no restart anywhere. Nine subscribers run on it. After such a death `get_value()` keeps serving the stale cache, so **every surface displays plausible frozen values instead of an error** — the worst failure mode for a camera instrument, and a direct answer to KICKOFF §7 constraint 5 for option A. **Hardware-confirmed by PI-014**: a forced subscriber exception froze both the cache-backed HTTP API and the SSE stream permanently and silently on the very first PUBLISH after the fault, for both a previously-seen key and a brand-new one — the worst-case failure mode claimed, decisively | `redis_controller.py:145-157,177,188-212,410`; subscribers at `mediator.py:20-22`, `simple_gui.py:222`, `cinepi_controller.py:147`, `app/api.py:205`, `app/main/events.py:112`, `status_broadcast.py:77`, `timekeeper.py:45` (dead, F-017); PI-014 |
| F-205 | high | confirmed | cinemate | correctness | **A 2-second `time.sleep` inside a Redis subscriber, on the shared listener thread.** On every `wb` change `redis_change_handler` sleeps 2 s and then emits `reload_browser`. Because `Event.emit` is synchronous, this stalls the other eight subscribers *and* the `_listen` loop itself — no pub/sub message is consumed for two seconds. The consequence is a full browser reload for a white-balance change, paid for with a system-wide state freeze | `app/main/events.py:108-111`, `redis_controller.py:155-157,188` |
| F-206 | — | confirmed | cinemate | strength | **PRESERVE — the project has already applied S06's rule once, successfully.** Control was moved out of the Socket.IO handlers to `POST /api/v1/cmd` so the browser sends the same command lines as the CLI and serial paths; the code says why, in place: *"behaviour cannot drift between them."* This is the single best precedent for the remediation plan — de-duplication by routing to one path, not by comment | `app/main/events.py:6-12,118-124` |
| F-207 | medium | confirmed | cinemate | gui | **The web GUI's liveness is a side effect of the HDMI redraw loop.** `emit_gui_data_change` is called only from `draw_gui`, which is called only from `SimpleGUI.run()` behind a `has_work` gate (`_fast_dirty or _slow_dirty or _vu_active()`). The browser therefore updates at the framebuffer's cadence and stops if that thread stops. Mitigating nuance, worth preserving deliberately: the emit sits *before* `draw_gui`'s `if not fb: return`, so the web GUI keeps working with no HDMI attached. **PI-015 confirmed the headless path on a real physical HDMI detach+reattach** (411 events, zero gaps >620ms over 55s) and **measured the actual cadence at ~7.5 Hz (132.6ms mean interval), not the ~12fps this finding assumed** — the only number this claim has ever had. F-223's claim that attaching a display restarts capture did not reproduce in the same test | `simple_gui.py:1764-1779,2094-2111,313-314`; PI-015 |
| F-208 | high | confirmed | cinemate | correctness | **The safe version of F-204 already exists in this repo.** `CinePiController._notify_resolution_change` wraps every callback in `try/except Exception` and logs with `logging.exception`. `RedisController.Event.emit` — which carries the entire live-state bus and nine subscribers — does neither. Same pattern, same codebase, ~900 lines apart. F-204's remediation is to copy the one that is already right | `cinepi_controller.py:1082-1087` vs `redis_controller.py:155-157` |
| F-209 | low | confirmed | cinemate | structure | The Socket.IO push channel is emitted from **three** modules (`app/main/events.py`, `simple_gui.py`, `app/__init__.py`) with no registry of what it carries. This review's own extractor scanned two of the three and wrongly reported two events as never emitted — the miss is the evidence | `app/main/events.py`, `simple_gui.py:297-314`, `app/__init__.py:22,29`; `harness/gui_field_extract.py` |
| F-210 | — | confirmed | cinemate | strength | **The Socket.IO event contract has not drifted.** Nine event names, every one emitted by the server and handled by the browser, both directions clean, no orphans either way. Against a review whose central finding is systemic drift, this boundary is in agreement — plausibly because it is small (9 names) and both sides ship together. Useful counter-evidence for ADR-001 | `harness/gui_field_extract.py` output; `app/templates/template.html` `socket.on` sites |
| F-211 | low | confirmed | cinemate | gui | **The web GUI shows the drop/sync warning *state* but not the *counts*.** It renders DROP and SYNC badges from the latched booleans `drop_frame_latched` and `frames_off_sync`, but `drop_frame_count`, `tc_hole_count`, `missing_frame_count`, `drop_frame_live`, `drop_frame_during_last_take` and `frames_in_sync` appear nowhere in the template. Recording-integrity detail is HDMI-only | `template.html:605-606`; 6 of the 20 HDMI-only fields, `harness/gui_field_extract.py` |
| F-212 | low | confirmed | cinemate | standards | `app/__init__.py:26` passes the raw string `'resolution_switching'` where `ParameterKey.RESOLUTION_SWITCHING.value` exists — a further instance of F-015 (the enum is convention, not enforcement) | `app/__init__.py:26`, `redis_controller.py:18` |
| F-213 | low | confirmed | cinemate | structure | `hasattr` guards on methods that definitively exist in the same repository — `simple_gui.set_socketio` and `cinepi_controller.add_resolution_change_callback`. If either is renamed the feature disappears silently instead of raising; the `else` branch for `set_socketio` also bypasses the deferred-event bookkeeping the real setter performs | `app/__init__.py:15-20`, `simple_gui.py:293-296`, `cinepi_controller.py:1076` |
| F-214 | medium | confirmed | cinemate | duplication | **Ten GUI label strings are duplicated and currently agree.** `EI`, `SHUTTER`, `FPS`, `WB`, `RES`, `EXP`, `RAM`, `CPU`, `TEMP`, `MEDIA` exist as Python literals in `populate_values` and as HTML text nodes in the template. Nothing checks them. One edit away from joining the ten boundaries that have already drifted | `simple_gui.py:789-833`, `app/templates/template.html` (`>EI<`, `>FPS<`, …) |
| F-215 | high | confirmed | cinemate | gui | **The HDMI GUI already has a declarative layout model — ADR-001 option C's data structure exists.** `setup_resources` builds three tables: `self.layout` (field → `{pos, size, font}`), `self.colors` (field → `{normal, inverse}`), and `left_section_layout`/`right_section_layout` — grouped sections with a label, ordered items, per-item text formatters and an optional `condition` predicate for visibility. That last one is almost exactly KICKOFF §7 option C's "one schema describes widgets, grouping, order, and per-surface visibility". The project invented it independently and did not share it. Caveat: the formatters are lambdas, so the structure is Python, not serialisable data — the *shape* is right, the encoding is not | `simple_gui.py:443-487` (layout), `:489-536` (colors), `:541-599` (sections) |
| F-216 | low | confirmed | cinemate | gui | Six display items are disabled by commenting them out inside the section tables (`resolution`, `bit_depth`, `exposure_time`, `resolution_cam1`, `bit_depth_cam1`, `frames_in_sync`). A table that already supports a `condition` predicate is being feature-toggled with `#` instead — and it collides with F-133's rule that commented code here is often load-bearing | `simple_gui.py:545,548,549,584,586,590` |
| F-217 | low | confirmed | cinemate | duplication | A **fifth** hand-sync comment: `template.html:216` says `/* _draw_status_box(crossed=True) strikes the SYNC box through */` — CSS naming a Python method as the thing it must stay consistent with. Same mechanism as F-007's colour comments, which sit 190 lines above it in the same file | `app/templates/template.html:216`, `simple_gui.py:1206` |
| F-218 | high | confirmed | cinemate | duplication | **The settings-editor action catalogue exists three times.** `ACTION_METHODS` in Python (46 entries), a hardcoded copy in the template's JavaScript (the same 46), and `CinePiController`'s 94 real public methods. The two hand-maintained copies agree *perfectly* — including agreeing on the same wrong entry, `set_log`. Two copies in exact agreement is not evidence they are right; it is evidence they were copied | `app/settings_editor.py:63-114`, `app/templates/settings_editor.html`, `cinepi_controller.py`; reproduced by `harness/gui_field_extract.py` |
| F-219 | high | confirmed | cinemate | dead-code | **The fix for F-118 is already written, and nothing calls it.** `GET /api/actions` resolves every catalogue entry against `_public_method_names(cinepi_controller)` and ships an `available` boolean per action — exactly the check that would flag `set_log`. The template never fetches the endpoint and the string `available` appears nowhere in its JavaScript. Zero consumers anywhere in `src/` or `docs/` | `app/settings_editor.py:115-119,292-305`; no `api/actions` reference outside its own route |
| F-220 | medium | confirmed | cinemate | duplication | **A hand-correction that was itself incomplete.** `settings_editor.py:56-62` says it is a *"Corrected copy of the mockup's original ACTION_METHODS catalog. Fixes the 3 entries that don't resolve via getattr()"* — and it did fix three (`erase`→`erase_drive`, `format`→`format_drive`, dropping `storage_preroll`), while leaving a fourth, `set_log`, wrong in both copies. The sixth hand-sync comment found in this review and the third that is wrong. Strongest single argument for S06 §3.3's mechanical check | `app/settings_editor.py:56-62,94`, F-118, F-219 |
| F-221 | — | confirmed | cinemate | strength | **PRESERVE — the recovery console is the best-engineered component in this review, and surface 4 must not be unified.** It states one rule and its reason (*"every import it makes is another way for it to die exactly when it is needed"*), honours it — venv-side settings validation runs as a **subprocess**, not an import — explains a deliberate missing systemd dependency in place (*"that coupling is the bug being fixed, not an oversight"*), builds numbered degradation ladders where the last rung still answers, and names its own test file. It answers one seventh of ADR-001 on its own: this surface's value is its isolation | `services/cinemate-recovery/cinemate-recovery.py:10-26,362-376`, `_test/test_recovery_console.py` |
| F-222 | high | confirmed | cinemate | test-gap | **381 tests across 27 files have never been executed by anything.** F-006 undercounts the loss by describing it as "27 pytest files". The distribution is also pointed: 86 of the 381 cover the recovery console — the component whose entire purpose is surviving failure — and 53 cover the wifi-hotspot ladder. The two most safety-critical subsystems are the best tested and the least verified | `_test/test_*.py` (381 `def test_`), `.github/workflows/docs.yml:9-13` |
| F-223 | — | confirmed | both | strength | **PRESERVE — and the strongest static evidence yet for PI-009.** Hot-plugging an HDMI display makes the GUI thread restart `cinepi-raw` *"so preview binds to the active display"*. It is properly guarded: a cooldown, a precondition that nothing is recording or writing (with a deferral log rather than a silent skip), and an exception handler that **re-arms** the pending flag instead of dropping the request. The implication for ADR-001 constraint 2: **cinepi-raw binds its preview to the display at process start and cannot rebind** — which is why a restart is the mechanism. Any option that introduces a second display client inherits this constraint | `simple_gui.py:401-433`, `cinepi_controller.restart_camera` |
| F-224 | low | confirmed | cinemate | structure | The HDMI GUI is **not** display-only: `simple_gui.py:428` calls `cinepi_controller.restart_camera()`. A rendering module owns a capture-pipeline lifecycle action. Correct behaviour (F-223), wrong home — and it means any renderer that replaces `simple_gui` must carry the restart-on-attach logic with it | `simple_gui.py:407-433` |
| F-225 | high | confirmed | both | method | **The review read mismatched branches for seven sessions — now corrected on operator instruction.** cinemate was read on `dev` throughout (correct: `origin/dev` is still `02b5a39`, the ledger branch's base, unmoved). cinepi-raw was read on `main` @ `774402c` per D2. cinepi-raw `dev` @ `ea96f2d` is **45 files and +7164 lines ahead**, including a near-rewrite of `dng_encoder.cpp` (687 lines), a new LOG-LUT subsystem, a new CCMP preview stage, 4 new `CONTROL_KEY_` macros and 6 new test targets. Every cinepi-raw figure in S01–S07 is a `main` figure | `git diff 774402c ea96f2d --stat`; D2 in `STATE.md` |
| F-226 | medium | confirmed | both | contract | **The cross-repo key contract is wider on `dev` than the ledger records.** Re-running `harness/redis_key_diff.py` against `dev`: **84 / 36 (28 macros + 8 direct) / 23 shared / 12 unreferenced**, against F-027's `main` figures of 84 / 32 / 19 / 12. The four new macros are the HDR family (`hdr_threshold_low`, `hdr_threshold_high`, `hdr_blend`, `hdr_gain_adder`) and cinemate already carries all four in `settings.jsonc`'s `arrays` block — so the HDR feature landed coherently on both `dev` branches. **The unreferenced count is unchanged at 12**: the drift did not grow | `cinepi/cinepi_state.hpp:50-53`, `harness/redis_key_diff.py` output on `dev`; cf. `findings/F-027.md` |
| F-227 | — | confirmed | cinepi-raw | gui | **`dev`'s DRM preview already enumerates and programs a spare overlay plane.** The `--same-hdmi` clone path walks `drmModeGetPlaneResources`, picks a plane that is not the primary's and supports the same fourcc on the second CRTC, and programs it with `drmModeSetPlane` — degrading with `"no spare plane for the second output; clone disabled"` when none is free. **Directly informs ADR-001 constraint 2:** cinepi-raw is already doing multi-plane DRM composition, and it already handles plane exhaustion gracefully. It does **not** settle PI-009 — this clones to a second CRTC, it does not overlay a GUI on the primary — but it proves the mechanism exists and is understood | `preview/drm_preview.cpp` (dev-only; absent from `main`) |
| F-228 | medium | confirmed | both | test-gap | **F-030 substantially understated the asymmetry.** cinepi-raw `dev` has **seven** `meson test` targets — `phase_lock_core`, `dng_pack`, `log_lut`, `log_encode_arg`, `ccmp_lut`, `ccmp_log_compose`, `ccmp_preview` — totalling **2803 lines** of test code, all wired into the build and run by `meson test`. The real comparison is not "one test next door" but **7 targets that run vs cinemate's 381 tests that never do** (F-222) | `cinepi/meson.build:66-130`, `tests/*.cpp`; cf. F-030, F-222 |
| F-229 | low | confirmed | both | gui | Both repos independently describe `--same-hdmi` as making the **preview and the GUI share one HDMI output** — cinepi-raw's own option help (*"Force preview and GUI to share the same HDMI output"*, present on both branches) and `docs/cli-user-guide.md:78` (*"Force both capture and controller GUI to share the same HDMI output"*). Two independent statements that the GUI and the preview do compose on one output. Neither says **how**, so PI-009 stands | `cinepi/cinepi_options.cpp:174-176`, `docs/cli-user-guide.md:78`, `docs/settings-json.md:381`, `cinepi_multi.py:533-537` |
| F-230 | low | confirmed | cinepi-raw | citation | S03's RAM auto-stop citation is stale against `dev`: the hard stop on the record path is at `cinepi_raw.cpp:225-229` (*"RAM pool exhausted — recording stopped"*), not `:200-212`, which on `dev` is clip-folder and timecode-origin code. `CODE-MAP-cinepi-raw.md` needs the correction | `cinepi/cinepi_raw.cpp:225-229` (dev) vs `CODE-MAP-cinepi-raw.md` |
| F-231 | low | confirmed | cinepi-raw | census | cinepi-raw `dev` is **29,438** lines of C/C++ against CENSUS §2's 24,051 for `main` — +22%. With the clone now on `dev`, **KICKOFF §6.2's table is the applicable one** and D2's "do not mix the two tables" warning is void | `find . -name '*.cpp' -o -name '*.hpp' …` on `dev` @ `ea96f2d`; KICKOFF §6.2 |
| F-232 | medium | confirmed | cinemate | duplication | **F-007 overstated the sync mechanism — it is weaker than "a comment".** Of the 16 colour custom properties in `template.html`'s `:root`, only **3** name their Python counterpart in a comment (`--zoom-hi`, `--drop`, `--sync`); the other 11 are undocumented parallel definitions that happen to agree. All 3 annotated pairs currently match and **zero tokens have drifted** — but 11 of 16 have no stated link at all, so a future edit has nothing to notice | `app/templates/template.html:24-41`, `simple_gui.py:22-48,489-536,1226`; reproduced by `harness/design_token_diff.py` |
| F-233 | low | confirmed | cinemate | duplication | `ZOOM_HIGHLIGHT_COLOR` is a **function-local** at `simple_gui.py:1226`, and the same literal `(255, 221, 0)` is re-typed as a bare tuple 580 lines later. The colour is stated three times in two files — twice inside Python | `simple_gui.py:1226,1806`, `template.html:28` |
| F-234 | — | confirmed | docs | strength | **PRESERVE — an accurate doc.** `docs/simple-gui-refresh-tuning.md` describes `target_fps = 12`, `min_frame_interval = 1/target_fps`, `slow_refresh_interval = 1.0`, `vu_decay_factor = 0.2` set in `run()`, and the `_redraw_event.wait(timeout=0.1)` idle fallback — **every one matches the code**, including which values are derived and should not be edited directly. It also states the operational limit (*"above 15 fps: usually not recommended with the current full-screen PIL-to-framebuffer path"*). Pre-answers part of S09 | `docs/simple-gui-refresh-tuning.md` vs `simple_gui.py:201-203,2079,2096` |
| F-235 | medium | confirmed | both | duplication | **Two independent RAM auto-stops, one per repo, with different mechanisms.** cinemate trips on `RAM_LIMIT_PERCENT = 80` (percentage of system memory); cinepi-raw trips on encoder `buffer_full()` (pool exhaustion) and logs *"RAM pool exhausted — recording stopped"*. Arguably legitimate layering — but nothing states the relationship, neither references the other, and an operator sees two different stop reasons for one condition | `cinepi_controller.py:257`, `cinepi_raw.cpp:220-230` (dev) |
| F-236 | low | confirmed | install | correctness | `camera-ready.sh` runs as `ExecStartPre` with `MAX_ATTEMPTS=30` at `RETRY_INTERVAL=1`, so it can hold the unit for **~30 s before `main.py` starts at all**. The unit is ordered `Before=plymouth-quit.service getty@tty1.service`, so that wait is in front of the console handoff too. A boot-time budget item for ADR-001 constraint 6 | `services/cinemate-autostart/camera-ready.sh:24-25`, `cinemate-autostart.service:3-4,18-19` |
| F-237 | — | confirmed | cinemate | gui | **Migration cost, measured.** `SimpleGUI` is 48 methods / 1913 lines, splitting as: **draw+layout 925 (48.4%)**, **state 636 (33.2%)**, display/framebuffer 241 (12.6%), socket.io 19 (1.0%), other 92. So a renderer swap rewrites ~925 lines and **preserves 636 lines of state logic unchanged** — the part surface 2 already reuses (F-203). KICKOFF §7 constraint 7's "how much of the 2129-line file must change" has a number: about 43% of the file, and it is a contiguous, identifiable half | `simple_gui.py`; AST measurement, method-by-method |
| F-238 | high | confirmed | cinemate | gui | **F-008 is partly outdated, and the correction is decisive for ADR-001.** The HDMI GUI is not uniformly absolute-positioned. `_top_row_layout` measures each of the six top-row groups' rendered text width, sums them, and distributes the remaining space between two anchors as an equal gap — a **justified flex row**, `justify-content: space-between` implemented in PIL, including a conditional HDR badge in the flow. `draw_left_sections` iterates the section spec, **honours each section's `condition` predicate**, centres each label on measured width, and advances a `y` cursor — a **vertical stack with conditional visibility**, whose gaps even adapt to whether the buffer VU bar is present. What is actually absolute is the *region anchors*, not the content inside them. **The HDMI GUI is a fixed grid of regions with content-driven flow inside the two busiest ones** | `simple_gui.py:1643-1671` (justified row), `:1238-1250` (conditional vertical stack), `:1234-1237` (VU-dependent gaps); cf. F-008, F-215 |
| F-239 | medium | confirmed | cinemate | gui | Corollary to F-238: KICKOFF §7's central framing — *"immediate-mode raster"* versus *"retained-mode declarative layout"* — **overstates the gap**. The HDMI renderer already measures content and computes positions from it in the two regions where layout is non-trivial. The remaining absolute positions are the bottom status row and the region anchors, which on a fixed-resolution instrument panel are a reasonable thing to pin. The tension is real but narrower than the ADR's framing assumes | `simple_gui.py:1643-1671,1220-1250`, KICKOFF §7 "The central tension" |
| F-240 | — | confirmed | docs | strength | **The documentation is in materially better shape than the code it documents.** Mechanically, across 50 files: **103 internal links, 0 broken**; **64 repo code citations, 0 unresolvable and 0 with an out-of-range line number**; **11 of 11 top-level `settings.jsonc` sections documented** with no phantom headings; **0 orphan rows** in `redis-keys.md`. Against a review whose central finding is systemic drift between code copies, the docs↔code boundary is the best-maintained one in the system | reproduced by `harness/docs_drift_check.py` |
| F-241 | medium | confirmed | docs | docs-drift | **13 of 84 `ParameterKey` members are absent from `redis-keys.md`**, and 7 of the 13 are one feature: `resolution_switching`, `resolution_target_mode/width/height/bit_depth`, `dynamic_resolution_enabled/active/desired_mode`. The rest: `audio_capture_gain_db`, `fps_phase_lock`, `trigger_mode`, `mode`, `packing`. F-014 recorded 18 by a different method; **zero orphan docs either way**, and the character is identical — the gap is one under-documented feature, not scattered rot | `docs/redis-keys.md` (71 table rows), `redis_controller.py:18` (84 members); `harness/docs_drift_check.py --only keys` |
| F-242 | — | confirmed | docs | strength | **The documentation copy of the controller catalogue is the *correct* one.** `controller-methods.md` names 43 methods and **all 43 exist on `CinePiController`** — while the two code copies (Python `ACTION_METHODS` and the settings-editor JavaScript) both carry `set_log`, which does not (F-218). Of four registries of the same truth, the prose is right and two of the three machine-readable ones are wrong | `docs/controller-methods.md`, `cinepi_controller.py`; `harness/docs_drift_check.py --only methods` |
| F-243 | low | confirmed | docs | docs-drift | `docs/cli-user-guide.md` cites `` `boot/firmware/config.txt` `` without the leading slash. Every other reference in the docs uses the absolute path. The **only** unresolvable citation among 100 across 50 files | `docs/cli-user-guide.md` |
| F-244 | medium | confirmed | docs | docs-drift | **15 of 50 docs are unreachable from the mkdocs nav** — reproducing F-004 mechanically — and two of them are not stubs: `image-circle.md` (159 LOC of real content) and `controller-methods.md` (73 LOC, and per F-242 the most accurate catalogue of controller methods in the repository). Five are 0-byte files and five nav lines are commented out. **The published site is missing its best method reference** | `mkdocs.yml`, `docs/`; `harness/docs_drift_check.py --only nav` |
| F-245 | medium | confirmed | docs | docs-drift | **`web-gui.md` asserts a drift-safety property that does not hold.** It says every control action posts a command line to `/api/v1/cmd` — *"the same dispatcher the CLI, serial and GPIO paths use, so the browser cannot drift from them."* True for CLI and serial; **false for GPIO**, which dispatches by `getattr(self.cinepi_controller, method_name)` at seven sites and never touches the HTTP path, bypassing `_dispatch_lock` entirely (F-025). The claim is right about the win the project actually made (F-206) and wrong about its scope | `docs/web-gui.md:9-11`, `gpio_input.py:263,398,467,551,601,635,647`, `cinepi_controller.py` `_dispatch_lock` |
| F-246 | medium | confirmed | both | docs-drift | **A setting that does not exist, named in three places — two of them inside the code.** `lock_dual_recording` appears in `changelog.md:11` as a settings key, in a **docstring** at `cinepi_controller.py:1347` (*"settings ``lock_dual_recording`` is true"*), and in a **comment** at `cli_commands.py:267`. It is in no settings file, no schema and no loader. The real control is `sensors.record_policy == "always_both"` (`cinepi_controller.py:1369`). One rename, three stale references — the docs-drift form of F-118, and the code's own prose is two thirds of it | `docs/changelog.md:11`, `cinepi_controller.py:1347,1369`, `cli_commands.py:267`; absent from `settings.jsonc`, `settings.schema.json`, `config_loader.py` |
| F-247 | low | confirmed | docs | docs-drift | `docs/changelog.md:11,66` refer to `settings.json`; the file is `settings.jsonc`. Only the changelog does this — the other 49 docs get it right | `docs/changelog.md:11,66` |
| F-248 | low | confirmed | docs | docs-drift | `web-gui.md:3-4` says the web GUI mirrors the HDMI GUI with *"same field layout, same status badges, same live preview."* The badges match, but 48 of 68 fields reach the template and the recording-integrity **counts** are HDMI-only (F-211). "Same" overstates it | `docs/web-gui.md:3-4`; `harness/gui_field_extract.py` |
| F-249 | low | confirmed | docs | docs-drift | `docs/simple-gui.md:5` — *"The browser-based web UI features basic settings and interactive controls."* — is an orphan sentence about a different surface, sitting under a heading about the HDMI GUI with no connection to the text around it. Residue from an earlier draft | `docs/simple-gui.md:5` |
| F-262 | — | confirmed | docs | strength | **Thin is not the same as drifted, and the review's own plan conflated them.** `PLAN.md` S09 flags `compiling-cinepi-raw.md` (15 LOC against a 29k-LOC repo) as thinnest-coverage-of-biggest-surface. Reading it: it documents `/home/pi/compile-raw.sh` and `FORCE_WIPE=1`, and **both are exactly right** — the installer generates that script with `FORCE_WIPE="${FORCE_WIPE:-0}"` and a `meson setup --wipe` branch. Its job is "how to rebuild", not "how it is built", and it does that job completely in 15 lines | `docs/compiling-cinepi-raw.md`, `cinemate-install.sh:796-798,806,849-850` |
| F-263 | low | confirmed | docs | method | **`changelog.md` could not be checked against git history.** `PLAN.md` S09 asks for a diff against the `cinemate-v3.1.1` tag; this clone has **no tags** (`git tag` is empty). Same class of limitation as D2's shallow cinepi-raw clone. The changelog's *content* was spot-checked and produced F-246 and F-247; its *completeness* against the commit log is unverified | `git tag` (empty), `PLAN.md` S09 |
| F-264 | high | confirmed | install | correctness | **The two repositories that move most are the two the installer does not pin.** `IMX283_DRIVER_REPO_REF` and `IMX585_DRIVER_REPO_REF` default to `6.12.y`; `LIBCAMERA_REPO_REF` defaults to `cinemate`. But `CINEMATE_REPO_REF` and `CINEPI_RAW_REPO_REF` default to **empty**, so both clone whatever the default branch happens to be that day — and the doc's `git clone` commands pin nothing either. S07b measured what that gap costs: `main` and `dev` of cinepi-raw differ by **45 files / +7164 lines**, including 4 `CONTROL_KEY_` macros of the cross-repo contract (F-226). **No manifest anywhere records a known-good cinemate↔cinepi-raw pairing.** With F-190 (zero pip pins), an install is not reproducible across two days | `cinemate-install.sh:67-70,89,93,72,388,875`, `docs/installation-steps.md:22,207,660`; cf. F-190, F-226 |
| F-265 | medium | confirmed | install | docs-drift | **The installer and its documentation have no step-level correspondence.** `cinemate-install.sh` runs **27 named, numbered steps** (`section "…"`, printed as `[NN]`); `installation-steps.md` is a manual-install narrative under **18 `###` sub-headings** with different names and a different order. Nothing maps one to the other, and the doc never cites a step number. An operator whose install fails at `[18] Writing runtime loader configuration` has no way to look that up. The doc *does* cross-reference the installer in prose (`:112`), so the link is understood — it is the granularity that is missing | `cinemate-install.sh:1822-1904` (27 `section` calls), `docs/installation-steps.md` (18 `###` under "Manual install"); zero occurrences of "step N" in the doc |
| F-266 | medium | confirmed | install | docs-drift | **The recovery console is absent from the entire 1061-line install document.** `cinemate-recovery` appears **zero times** in `installation-steps.md` — not in its "Cinemate services" section (which covers the other four units under `####` headings), not anywhere. The installer installs it and its systemd unit. It is documented in `recovery-console.md` and `system-services.md`, both published — but a reader following the install never learns that a console exists on :8080 for when the venv is broken or redis is down. Per F-221 that is the best-engineered component in the system, and its whole value is being reachable when nothing else is | `docs/installation-steps.md` (0 hits), `services/cinemate-recovery/`, `docs/recovery-console.md`, `docs/system-services.md` |
| F-267 | — | confirmed | install | strength | **Everything mechanically checkable between the installer and the install doc agrees.** Sensor-driver repo URLs and their `6.12.y` ref, libcamera's `cinemate` branch, 7 shared GitHub URLs, 16 shared absolute paths, and — checked both directions — an **exactly matching 17-name systemd unit set**. The subjects the step-name matcher flagged as missing (`redis-plus-plus`, `libtiff`, the venv, `lgpio`, the loader config, `.asoundrc`, post-processing configs, the overclock) are all covered in the doc's prose; only the *headings* fail to line up (F-265). For a 1916-line installer against a 1061-line document, that is a strong result | `harness/` literal diff over `cinemate-install.sh` × `docs/installation-steps.md` |
| F-268 | high | confirmed | cinemate | correctness | **F-025 settled from source, and it is broader than recorded — PI-007 step 1 is discharged with no hardware.** `_dispatch_lock` lives in `CommandExecutor` (`cli_commands.py:21`), is taken with a 2-second timeout (`:218`), and serialises **three** input paths: the CLI thread, serial, and HTTP `/api/v1/cmd`. **Six** modules bypass it entirely by calling the controller through `getattr`: `gpio_input.py`, `analog_controls.py`, `rotary_encoder.py`, `i2c/quad_rotary_controller.py`, **`storage_preroll.py`** and **`simple_gui.py`** — the last two not named in F-025. A GPIO press and a web command can therefore execute `set_fps` concurrently with nothing between them | `cli_commands.py:21,218,257`, `main.py:786,790`, `app/api.py:120`; bypassing callers listed by `getattr(...cinepi_controller...)` |
| F-269 | high | confirmed | cinemate | correctness | **`CinePiController` has almost no internal locking to fall back on.** Three locks across **151 methods**, with **9 acquisition sites** total: `parameters_lock_obj` (4 sites, `:1861-2007`, around shutter-angle/exposure state), `_storage_profile_restart_lock` (4), `_resolution_change_pace_lock` (1). They guard three specific operations, not the controller's state in general. Combined with F-268 this is the whole concurrency story: **the only general serialisation in the system is a lock two thirds of the input surfaces do not take, and the object it protects does not protect itself** | `cinepi_controller.py:55,135,138` (declarations), `:1096,1258,1291,1302,1306,1861,1868,1910,2007` (all 9 acquisitions) |
| F-270 | — | confirmed | cinemate | readability | **`cinepi_controller.py` is wide, not deep — which changes what should be done about it.** 2626 lines is **151 methods on one class** (94 public, 57 private, zero `@property`), averaging ~16 lines each. Only `__init__` (239 lines) is genuinely oversized; the next largest is 108. This is a broad facade, not tangled logic, and it is why six sessions could defer reading it without the review stalling. A decomposition proposal should split it **by concern into modules**, not by extracting long methods — there are almost none to extract | `cinepi_controller.py:40-2626`; AST measurement |
| F-271 | high | confirmed | cinemate | correctness | **Saving from the web settings editor destroys every comment in `settings.jsonc`, with no backup and no warning.** `put_settings` writes `json.dumps(settings, indent=2)` — plain JSON — over a `.jsonc` file that is **74 comment lines out of 386 (19%)**, including the section banners and the inline explanations of what each key does. KICKOFF §9 principle 8 states those comments *"are part of the product"*. The word `backup` does not appear anywhere in `settings_editor.py`. **The correct implementation is in the same repository:** the recovery console's `write_config_file` writes the user's **raw text** (comments intact) and backs up first, with a docstring reading *"Back up, then atomically replace. The order is not negotiable."* Same failure shape as F-204/F-208 — the right version already exists next door | `app/settings_editor.py:186,173-215` vs `services/cinemate-recovery/cinemate-recovery.py` `write_config_file`, `backup_file`, `BACKUP_KEEP`; `settings.jsonc` (74 of 386 lines) |
| F-272 | — | confirmed | cinemate | test-gap | **The 381 tests run clean off-hardware — PI-002 is largely answered, and the answer is better than the queue assumed.** `python3 -m pytest _test/` completes in **~2 seconds** with **381 passed + 241 subtests, zero failures**, needing only nine pip packages (`pytest psutil redis pillow numpy termcolor flask flask_socketio pyudev`) and **no Raspberry Pi**. The review recorded these as never having been executed by anything (F-006, F-222) and PI-002 assumed a portable/hardware split had to be discovered. There does not appear to be one. **Two batches change:** `REMEDIATION-PLAN.md` B4.6's pytest job can gate at zero immediately instead of starting in discovery mode, and B6's `requirements.txt` split line is now known rather than pending | run on `claude/remediation-b3-correctness` @ `origin/dev` 02b5a39; 388 passed after B3.1/B3.2a added 7 tests |
| F-273 | — | confirmed | cinemate | correction | **Correction to F-023, found while fixing it: `USBMonitor` has no `stop()` at all.** F-023 says `cleanup()` "never calls `usb_monitor.stop()` … although both exist". The method it saw is `AudioMonitor.stop()` (`usb_monitor.py:528`); the class `main.py:611` actually instantiates is `USBMonitor` (`:538`), which defines no shutdown path whatsoever. So the fix is not "call the method that exists" — it is a design decision about whether that class should have one. Left unwired in B3.4 for exactly that reason | `usb_monitor.py:100,528,538-539`, `main.py:611`; cf. F-023 |
| F-274 | medium | confirmed | cinemate | dead-code | **`Mediator.handle_fps_change` and `handle_shutter_a_change` do nothing at all.** Both are subscribed to `redis_parameter_changed` in `Mediator.__init__`, and each body consisted *entirely* of a `get_value()` call whose result was discarded — removing the dead read left empty function bodies. So two of the nine subscribers on the live-state bus are no-ops that have been running on every redis change. Distinct from F-102 (methods with no subscriber): these have subscribers and no effect | `mediator.py:21-22,169-176` |
| F-275 | — | confirmed | cinemate | test-gap | **The test suite is order-dependent by construction.** `test_simple_gui_preview_guide.py` and `test_log_encode_badge.py` install `flask_socketio` into `sys.modules` as `SimpleNamespace(SocketIO=object)`, with no `emit` and no cleanup. `sys.modules` is process-wide, so whichever test imports first decides what that module means for the whole run — a new test importing `app.main.events` breaks collection for the entire suite depending on collection order. Found by hitting it. Caveat on F-272 and on `REMEDIATION-PLAN.md` B4.6: a CI job that shards or randomises could go red on ordering alone | `_test/test_simple_gui_preview_guide.py:9`, `_test/test_log_encode_badge.py`, `app/main/events.py:1` |
| F-276 | high | confirmed | cinemate | correctness | **`pyserial` is imported by live code and installed by nothing.** `serial_handler.py:8` does `import serial`, and `main.py:789` constructs `SerialHandler` unconditionally on every boot. The package appears in **no pip list and no apt list anywhere in the repository** — not the installer's literal, not `requirements.txt`, not `installation-steps.md`. Same undeclared-and-therefore-unguaranteed shape as `flask` (F-186), found by the same method. Neither package is declared by anything that intends to provide it. **Hardware-confirmed by PI-004**: `pip show pyserial` on a clean install reads `Required-by: binho-host-adapter, pyftdi` — transitive via the GPIO/sensor stack, not `adafruit-blinka` as guessed, but transitive-only either way | `serial_handler.py:8`, `main.py:789`; zero hits for `pyserial`/`python3-serial` in `cinemate-install.sh`, `requirements.txt`, `docs/`; PI-004 |
| F-277 | medium | confirmed | cinemate | dead-code | **Nine pip packages are installed on every camera and imported nowhere** — `inotify_simple`, `keyboard`, `luma.oled`, `pigpio-encoder`, `rpi_hardware_pwm`, `sounddevice`, `sugarpie`, `sysv_ipc`, `watchdog`. Checked across `src/`, `services/`, `scripts/` and `_test/`, not just `src/`. F-032 recorded 7 of 11 by a narrower method; the AST-derived figure is 9. Eight were already unreferenced on `dev`; `sugarpie` became unreferenced when B4 removed an unused import from `simple_gui.py` | AST pass over every top-level import in `src/` vs `cinemate-install.sh:922-927`; cf. F-032, F-187, F-188 |
| F-278 | — | confirmed | cinemate | test-gap | **The portable dependency set is now known exactly, closing PI-002 completely.** A clean venv built from nine runtime packages plus `pytest`/`jsonschema` runs the entire suite — 386 tests, 244 subtests — with no Raspberry Pi and nothing else installed. PI-002 was written to *discover* a portable/hardware test split; there is none to discover. `src/` has exactly **21 third-party top-level imports**, of which 9 are portable and 12 are hardware-bound | clean-venv run on `claude/remediation-b6-deps`; cf. F-272, PI-002 |
| F-279 | high | confirmed | install | correctness | **`bootstrap_sudo()`'s `sudo -v` hangs forever, no error, no timeout, on a stock Raspberry Pi OS image.** The pi user carries both the image's default `%sudo ALL=(ALL:ALL) ALL` group rule and a `NOPASSWD:ALL` per-user rule; plain `sudo <cmd>` resolves to the NOPASSWD path but `sudo -v` (credential-refresh mode) does not, and blocks on a password prompt no non-interactive installer session can answer. Found running a real clean install on a blank Bookworm Lite card — the installer hung at step `[01] Validating environment`. Fixed, merged #138 (`6a15ed8`): `sudo -n true` exercises the same NOPASSWD path a real command uses and fails fast instead | `cinemate-install.sh:202-210` (fixed); reproduced on a genuinely blank Raspberry Pi OS Lite Bookworm 64-bit image, PI-004 |
| F-280 | high | confirmed | install | correctness | **The pinned `raspi-firmware` download 404s.** `align_pi5_kernel_baseline()` pins `raspi-firmware_1.20260521-1~bookworm_all.deb` against the apt "untested" pool, which retains only the single newest upload — the pinned build had already been superseded there. The exact pinned version was still available in the "main" pool (built packages graduate there and stay). Fixed: point at "main" instead, same pinned version, no version bump | `cinemate-install.sh:445-458` (fixed); reproduced on the same blank-card install as F-279, PI-004 |
| F-281 | high | confirmed | install | correctness | **`configure_settings_json()` destroys every comment in `settings.jsonc` on every single install**, not only when the web settings editor is used (F-271). It unconditionally parses the file, sets a handful of hotspot/HDMI keys, and writes the whole structure back with `json.dumps()` — even when the installer's own defaults already match what's committed (the common case, since `HOTSPOT_NAME`/`HOTSPOT_PASSWORD`/`HDMI_PORT_CAM0`/`HDMI_PORT_CAM1` default to exactly what `settings.jsonc` ships with). Confirmed on the fresh blank-card install: on-disk `settings.jsonc` came out at 482 lines with zero `//`/`/* */` comments, against the tracked file's 386 lines and 71 comments. Same failure shape as F-271, a different code path entirely (the installer itself, not the web editor). Fixed, merged #138 (`6a15ed8`): skip the write entirely when the requested values already match the file; only fall back to the lossy rewrite — now with a warning and a `.bak` of the original — when a value is genuinely being customized | `cinemate-install.sh:1633-1745` (fixed); reproduced on the same blank-card install, PI-004 |
| F-282 | low | confirmed | install | correctness | **`tuning_file_override.path` for cam0/cam1 in `settings.jsonc` was a relative path** (`resources/tuning_files/imx477.json`), passed verbatim to cinepi-raw's `--tuning-file` flag with no resolution against a known base directory (`cinepi_multi.py:425-426`). It only works today because `cinemate-autostart.service` pins `WorkingDirectory=/home/pi/cinemate`; a manual launch (the `cinemate` alias, or running `main.py` from a different directory) would resolve it against the wrong CWD. The default (non-override) tuning path is already hardcoded absolute (`/home/pi/libcamera/...`); the override paths now match that convention | `settings.jsonc` (fixed, both cam0/cam1), `cinepi_multi.py:423-426` (unresolved relative-path consumer, not itself changed) |
| F-283 | medium | confirmed | cinemate | correctness | **`systemctl restart cinemate-autostart` reliably hangs and lands the unit in `failed`.** `ExecStopPost=cinemate-console-handoff.sh` does not return within `TimeoutStopSec=5` on every restart observed this session (reproduced 5+ times, across two different sensors and two different installs — not sensor- or install-specific), and systemd kills it at timeout, leaving the unit `failed` rather than restarted. Recoverable every time via `systemctl reset-failed` + `start`, but the plain `restart` verb an operator would reach for does not work as-is. **Root-caused 2026-08-25**: not the `--no-block`/`Conflicts=` deadlock the initial desk diagnosis proposed — `bash -x` showed the script blocking on an interactive PolicyKit prompt (`AUTHENTICATING FOR org.freedesktop.systemd1.manage-units`) that `ExecStopPost`'s non-interactive context can never answer. **Fixed, merged #135 (`4f765c4`)**: `sudo -n` on the `systemctl start` calls skips the prompt. A second, narrower, unfixed race remains open: a separate `systemctl start getty@tty1` call inside `main.py`'s `restore_local_console_prompt()` can still collide with `Conflicts=getty@tty1.service` during an in-flight restart, occasionally landing the unit `inactive` instead of `active` (self-recovers on the next restart; never hangs, never `failed`). Four mitigations tried and rejected — see #135's description and `PI-RESULTS-2026-08-25.md` | `services/cinemate-autostart/cinemate-autostart.service` (`TimeoutStopSec=5`, `ExecStopPost=...cinemate-console-handoff.sh`); `services/cinemate-autostart/cinemate-console-handoff.sh` (fixed); `src/main.py` `restore_local_console_prompt()` (residual race, not fixed); reproduced repeatedly, PI-014/PI-007/PI-011/PI-016 sessions, root-caused and fixed 2026-08-25 |
| F-284 | medium | confirmed | cinemate | correctness | **The `unmount`/`mount` CLI command pair is not a reliable round-trip for the NVMe RAW volume.** `unmount` succeeds, but the following `mount` fails with `mount_drive(): no partition labelled RAW found` even though `blkid`/`lsblk` find `/dev/nvme0n1 LABEL=RAW` instantly at the block-device level. Recovered manually with `sudo mount -L RAW /media/RAW`, after which cinemate's own `is_mounted`/`space_left` picked it up correctly. Reproduced once during PI-008, then **10/10 on a dedicated repro session (2026-08-25)** — not a rare flake. **Root-caused**: not `mount_drive()` itself — `_blkid_value()` (`ssd_monitor.py:555`). Mechanism is stale/short-circuited result, not the `timeout=1.0` firing (timed at 3-18ms across all 10 reproductions, ruled out): `fresh=True`'s `blkid -p` correctly raises `PermissionDenied` (unprivileged `User=pi`) and falls through, but its `blkid -c /dev/null` fallback needs the same raw access it doesn't have either and returns **empty output with exit 0** on this blkid version — the old code treated that as a final result and never reached the cache-backed query one step further that actually works. This NVMe has no partition table (`LABEL=RAW` lives directly on `/dev/nvme0n1`), which is why the raw-probe permission wall is hit at all. **Fixed, merged #136 (`2c73b22`)**: a command must return a non-empty value to count; empty results fall through instead of terminating the search. Verified 10/10 mount cycles clean after the fix (0/10 before) | `src/module/ssd_monitor.py` `_blkid_value()` (fixed; original evidence line pointed at `mount_drive()`, corrected 2026-08-25), `_find_raw_device()` (now logs enumeration failures, was a silent `return []`); reproduced 10/10 during PI-008 follow-up and the dedicated repro session, PI-VERIFICATION-QUEUE PI-008 |
| F-285 | high | confirmed | cinemate | correctness | **Hardware proof that F-268/269's unserialised analog-pot path fully starves explicit commands, not occasionally collides with them.** With a Grove Base HAT pot wired to `iso` and continuously turned, 20 separate explicit `set iso 6400` calls over `/api/v1/cmd` — sent every 350ms for 14s — never once landed as the live value; `redis-cli MONITOR` showed 107 pot-driven `iso` writes in the same window (~1 every 130ms) and zero occurrences of "6400". A single isolated `set iso 6400` with no rapid-fire competition, pot just sitting live, also failed: the API returned `ok` (accepted, briefly applied) but the value read back as the pot's position one second later, with no error surfaced anywhere. `AnalogControls` (`analog_controls.py`) bypasses `CommandExecutor._dispatch_lock` per F-268 and simply out-polls any external writer — this is not a rare race, it is the default outcome whenever a pot is connected and moving. **Diagnosed 2026-08-25**: a dispatch lock alone cannot fix the isolated case — it is a later, well-ordered, unlocked write, not a race for the same slot; the pot's own debounce compares a fresh reading against its own last-*dispatched* value, with no notion of the live system value having changed. **Fix proposed and implemented, PR #140 (not yet merged)**: (a) the pot now takes the same dispatch lock, (b) a movement gate requires genuine ADC movement since the last dispatch before re-dispatching, (c) the command surface reads back what actually took effect instead of blindly reporting `ok`. Hardware-verified: starve case now honestly reports the mismatch instead of lying; isolated case now genuinely sticks | `src/module/analog_controls.py` (continuous poll thread, bypassed the dispatch lock per F-268, fixed in #140), `src/module/cli_commands.py` (read-back confirmation added, #140); PI-007, real hardware, Grove Base HAT |
| F-286 | medium | confirmed | cinemate | correctness | **`choose_resolution()`'s tie-break can never select a higher-bit-depth mode at a sensor's maximum resolution, through the normal command surface, whenever a same-resolution higher-fps-max lower-bit-depth mode exists.** It picks the eligible mode maximizing `(area, fps_max)` in that order with no bit-depth term (`dynamic_resolution.py:167-169`); on imx477, 4056x3040 10-bit (fps_max 14.0) and 4056x3040 12-bit (fps_max 11.72) tie on area, so 10-bit wins for any fps that makes 12-bit eligible at all — there is no fps value where 12-bit can win that tie. Forcing 12-bit at max resolution required a temporary source-level bypass of `dynamic_resolution_enabled`, which itself has **no CLI or redis-write toggle** — it is a hardcoded `True` in `cinepi_controller.py:110`, published to redis for display only, never read back. "Highest resolution" and "highest bit depth" are not jointly reachable via `set resolution` on any sensor with this mode shape. **Confirmed as written, 2026-08-25** (the `IMX477_MODES` test fixture used to verify was reconstructed from PI-016's notes at handoff time; replaced with the real `cinepi-raw --list-cameras` table before merging — the reconstructed fps_max numbers at 4056x3040 matched exactly). **Fixed, merged #137 (`d35dfef`)**: an explicit, sustainable request for the desired mode is now honored directly, ahead of the tie-break; the missing toggle is wired up (CLI + both settings-editor catalogue copies). **Tie-break for genuine downgrades fixed, PR #139 (not yet merged)**: bit depth now ranks above `fps_max` once eligibility is already satisfied. Hardware-verified past the redis/GUI display layer: recorded a take at the forced 12-bit mode and parsed the DNG's own TIFF tags directly (`BitsPerSample=12`, `ImageWidth=4056`, `ImageLength=3040`) | `src/module/dynamic_resolution.py:132-177` (`choose_resolution`, fixed #137 + #139), `cinepi_controller.py:110` (`dynamic_resolution_enabled`, toggle wired #137); PI-016, real hardware, imx477 |
| F-287 | medium | confirmed | cinepi-raw | test-gap | **cinepi-raw has a seven-target test suite and no CI to run it.** `cinepi/meson.build` defines `phase_lock_core`, `dng_pack`, `log_lut`, `log_encode_arg`, `ccmp_lut`, `ccmp_log_compose` and `ccmp_preview` as `test()` targets. They are unguarded by the README build's `-Dtest=false` (that is rpicam-apps' own option), so every normal build produces them — but the repository has **no `.github/workflows` directory at all**, so nothing has ever run them automatically. They passed for the first time on 2026-08-24, by hand, during the Phase C session. Supersedes F-030's "a working `meson test` target" and F-228's count. The asymmetry is the finding: cinemate now runs five checks on every PR (#131) and cinepi-raw runs none | `cinepi/meson.build:70,78,87,96,107,120,130`; absence of `.github/workflows` on `dev` at `bc63598`; first run reported 2026-08-24 |
| F-288 | high | confirmed | cinemate | correctness | **The settings editor cannot save `config.txt` — it writes a temp file into a root-owned directory.** `settings_editor.py:365` does `tempfile.mkstemp(dir=str(dest.parent))` where `dest` is `/boot/firmware/config.txt`. The web app runs as `User=pi`; `/boot/firmware` is root-owned, so `mkstemp` raises `EACCES` before a single byte is written. The settings.jsonc save at `:268` uses the byte-identical pattern and works only because `/home/pi` is pi-writable — the two paths were written together and only one of them was ever exercised. Reported from the field: *"Save failed: Could not write /boot/firmware/config.txt: [Errno 13] Permission denied"* | `src/module/app/settings_editor.py:365` (`mkstemp`), `:268` (the working twin), `app/boot_config.py:25` (`CONFIG_TXT_PATH`) |
| F-289 | high | confirmed | install | install-drift | **Nothing in either repository installs or enables `avahi-daemon`, so `cinepi.local` only ever worked by accident of the base image.** `configure_hostname_and_i2c()` runs `hostnamectl set-hostname cinepi` and stops there — a grep for `avahi`, `mdns` or `nss-mdns` across the whole repo (excluding `system-review/`) returns **zero** hits. mDNS publication is what makes `<hostname>.local` resolvable, and Raspberry Pi OS Lite does not guarantee it. `hostnamectl` alone also leaves `/etc/hosts` carrying the previous `127.0.1.1` entry. Reported from the field after clean installs on blank Lite images | `cinemate-install.sh:1318-1326` (`configure_hostname_and_i2c`), `:17` (`TARGET_HOSTNAME`); absence of any avahi reference repo-wide |
| F-290 | medium | confirmed | cinemate | correctness | **The web GUI's preview stream is told to reconnect before the new stream exists.** The client-side machinery is correct and already present — `template.html:914` `socket.on('reload_stream', () => reloadStreams())` re-attaches each `<img>` with a cache-busting query. But `reload_stream` is emitted from `emit_resolution_change` (`app/__init__.py:29`), which fires from `_notify_resolution_change` at `cinepi_controller.py:1749` — inside `_apply_resolution_mode`, on the line **before** `_schedule_resolution_switch_complete`. So the browser reconnects at the moment the switch is *initiated*, while cinepi-raw is still restarting; the `<img>` fails and stays dead until a manual page reload. The completion hook it should ride on already exists on the next line | `src/module/app/__init__.py:29`, `cinepi_controller.py:1749` and `:1750` (`_schedule_resolution_switch_complete`), `app/templates/template.html:914-925` |
| F-291 | medium | unverified | cinemate | correctness | **"Restart Cinemate" in the settings editor does not restart it.** The chain exists: `settings_editor.py:289-294` fires `threading.Timer(0.4, cinepi_controller.restart_cinemate)`, and `cinepi_controller.py:2378` does `os.execl(sys.executable, sys.executable, *sys.argv)`. Not yet root-caused. Leading candidates, in order: `os.execl` from a `Timer` thread while the Flask/SocketIO server holds listening sockets; `sys.argv[0]` being relative with a CWD that `exec` does not preserve; and `sys.executable` having changed from the venv interpreter to `/usr/bin/python3` when the venv was dropped (#138). Needs `journalctl -u cinemate-autostart -f` while the button is pressed | `src/module/app/settings_editor.py:288-294`, `cinepi_controller.py:2378-2382` |
| F-292 | medium | confirmed | cinemate | gui | **The settings editor's GPIO pane edits only the first 3 buttons and first 2 switches, and tells the operator so in a banner.** `settings_editor.html:1730` renders *"This file has more bindings than this page can edit"*. The pass-through is honest and safe — unshown bindings survive a save — but a settings editor that cannot edit the settings is a stopgap presented as a feature. Reported from the field as *"which seems strange"* | `src/module/app/templates/settings_editor.html:1730` |
| F-293 | medium | confirmed | cinemate | gui | **The GPIO binding editor's column model does not match how bindings are actually shaped.** Operator's specification: column 1 the GPIO pin, column 2 the action — **including `press`, currently omitted from the dropdown** — and column 3 the method, under a plainer name. One pin must be able to carry several actions, adding a method row per action, with action and method rows aligned. The present layout inverts this and cannot express multiple actions per pin | `src/module/app/templates/settings_editor.html` (GPIO pane), `settings.jsonc` `hardware_controls` |
| F-294 | medium | confirmed | cinemate | gui | **"Rec tally" should be "GPIO out", shaped like the input editor.** Same column model — GPIO first, then method — which as a side effect makes multiple tally pins expressible, currently impossible. The heading should drop "rec tally" entirely: the section is about outputs, of which tally is one use | `src/module/app/templates/settings_editor.html` (rec tally pane) |
| F-295 | low | confirmed | cinemate | gui | Raw pane: "download selected" and "delete selected" sit above the list they act on; they belong at the bottom, after the selection they operate over | `src/module/app/raw_files.py`, `app/templates/settings_editor.html` (raw pane) |
| F-296 | medium | unverified | cinemate | gui | **The adaptive hamburger menu does not work on a phone.** Reported from the field; the specific failure (does not open, opens behind content, wrong breakpoint) is not yet characterised. Needs a device or a narrow-viewport reproduction before a fix is designed | `src/module/app/templates/settings_editor.html`, `app/templates/template.html` |
| F-297 | medium | confirmed | cinemate | gui | **The web GUIs reflow instead of scaling.** Operator's requirement: the layout should adapt to whatever rectangle it is given — the top row stays the top row, and the left and right grey-box columns stay columns rather than collapsing into a single stacked flow. This is the web GUI's half of ADR-001's harmonisation question: the HDMI GUI scales its regions, the web GUI reflows them, and that is why the two surfaces stop resembling each other at narrow widths | `src/module/app/templates/template.html` (grid/flex layout), cf. `decisions/ADR-001-gui-harmonization.md` constraint 2 |
| F-298 | medium | confirmed | cinemate | standards | **A sensor's advertised `fps_max` cannot be corrected, only added to.** `choose_resolution()` picks the largest-area mode whose `fps_max >= requested_fps` (`dynamic_resolution.py:158-169`), and that ceiling comes from parsing `cinepi-raw --list-cameras` (`sensor_detect.py:286`). It is an electrical property of the sensor and says nothing about what this storage and this CPU sustain in practice. The extension point for fixing that **already exists and is half-built**: `image_capture.custom_modes` (`settings.jsonc:203`, read at `sensor_detect.py:78,376`) lets a user declare a mode carrying an explicit `fps_max` — but the merge only ever `append`s (`:381`), so it can add a mode and cannot correct one that was detected. Two consequences: the per-resolution fps ceiling is not tunable, and an entry whose dimensions match a detected mode silently produces a duplicate rather than an override. Its schema entry is bare `{"type": "object"}` (`settings.schema.json:321`), the one unconstrained block in a file where #131 set `additionalProperties: false` in all 25 other places, so a typo inside it is unchecked | `src/module/dynamic_resolution.py:158-169`, `sensor_detect.py:78,286,374-392`, `settings.jsonc:203`, `settings.schema.json:321` |
| F-299 | high | confirmed | docs | docs-drift | **The install documentation still builds the virtualenv that #138 removed.** `installation-steps.md:611-613` creates `~/.cinemate-env`, sources it, and appends the activation to `.bashrc`; `:619` writes the sudoers rule `pi ALL=(ALL) NOPASSWD: /home/pi/.cinemate-env/bin/*` that #138's `configure_sudoers()` change **actively deletes**; `:728` adds a `cinemate-env` alias. Four more files reason about it: `installation-steps.md:116,154` on meson picking the venv Python, `overclocking.md:116-119` telling the reader to `deactivate` first, and `recovery-console.md:35-36`/`hotspot-logic.md:8` describing a "broken virtualenv" as a supported failure mode. Anyone following the docs today builds an environment the installer no longer creates and the code no longer looks in | `docs/installation-steps.md:116,154,600,611-613,619-620,728`, `docs/overclocking.md:116-119`, `docs/recovery-console.md:35-36`, `docs/hotspot-logic.md:8`; cf. `cinemate-install.sh` `install_python_environment()` after #138 |
| F-300 | high | confirmed | docs | docs-drift | **The manual install instructions and the installer produce different — and differently broken — Python environments.** `installation-steps.md:640-646` pip-installs 23 packages by hand. Against `requirements.txt` + `requirements-hardware.txt` as #133 left them, the hand list **omits `pyserial`** — which `serial_handler.py:8` imports and `main.py:789` constructs unconditionally, the exact defect F-276 recorded and #133 fixed — and **omits `lgpio`**, which `requirements-hardware.txt` annotates as *"NOT optional despite the installer's INSTALL_ALT_GPIO_BACKEND flag calling it so"*. It also installs nine packages the review found imported nowhere: `luma.oled`, `pigpio-encoder`, `rpi_hardware_pwm`, `watchdog`, `keyboard`, `sounddevice`, `inotify_simple`, `sysv_ipc`, `sugarpie` (F-277, F-032). Following the doc therefore reproduces two fixed bugs and installs nine unused packages | `docs/installation-steps.md:640-646` vs `requirements.txt`, `requirements-hardware.txt` |
| F-301 | medium | confirmed | docs | docs-drift | **F-265 quantified: `main()` runs 45 named steps and the install document mirrors none of them.** `bootstrap_sudo` → `print_post_install_notes`, in a fixed order, including whole subsystems the prose never mentions by name — `align_pi5_kernel_baseline`, `install_imx283_support`, `install_imx585_support`, `install_sensor_tuning_overrides`, `configure_rp1_overclock`, `refresh_pi5_boot_handoff`, `configure_audio_rtprio`, `seed_redis_defaults`. A reader cannot tell which manual section corresponds to which installer step, so neither can be checked against the other | `cinemate-install.sh` `main()` (45 steps), `docs/installation-steps.md` |
| F-302 | medium | confirmed | docs | docs-drift | **The dependency layout #133 introduced is documented nowhere.** `requirements.txt` (portable), `requirements-hardware.txt`, `requirements-dev.txt`, `docs/requirements-docs.txt` and the `versions.env` pairing manifest are all live and unmentioned in any doc or README — a grep for "requirements" across `docs/` and `README.md` returns only the phrase "hardware requirements" in an unrelated sense. The split was the answer to F-002/F-003 and no reader can discover it | `requirements*.txt`, `versions.env`; absence across `docs/*.md`, `README.md` |
| F-303 | medium | confirmed | docs | docs-drift | **Neither repository documents its CI.** cinemate has run five checks on every pull request since #131 — `pytest`, `ruff`, `shellcheck`, `mkdocs` and contract drift with two ratchets — and cinepi-raw gained its first workflow in B10.6/#60. Nothing in either README or doc set says the checks exist, what they protect, or that raising a ratchet is never the fix. B4 was scoped as "what makes this review durable"; an undocumented gate is a gate the next contributor works around | `.github/workflows/checks.yml`, cinepi-raw `.github/workflows/` (B10.6); absence across both doc sets |
| F-304 | medium | confirmed | cinepi-raw | docs-drift | **cinepi-raw's README opens with an apt install that cannot succeed.** `README.md:17` runs `sudo apt install -y python-pip git python3-jinja2 ffmpeg`. `python-pip` is the Python 2 package, gone from Debian since Bullseye and absent on the Bookworm images this project targets — the command aborts and takes the rest of the step with it. `:51` gets it right with `python3-pip`, so the two halves of the same document disagree | `README.md:17` vs `:51` (cinepi-raw `dev`, `bc63598`) |
| F-305 | medium | unverified | docs | docs-drift | **The recovery console's documented degradation ladder is described in terms of a virtualenv that no longer exists.** `recovery-console.md:35-36`: *"If the Cinemate virtualenv is intact, it runs the exact same loader Cinemate itself uses… If the virtualenv is broken, it falls back to a plain JSON syntax check."* After #138 both branches run on the same system interpreter. The *mechanism* — validate by subprocess, fall back to a plain parse — may well survive intact, which is why this is `unverified`: read `cinemate-recovery.py` before rewriting, and preserve F-221's recorded strength rather than flattening the ladder | `docs/recovery-console.md:35-36`, `services/cinemate-recovery/cinemate-recovery.py` |
| F-306 | low | unverified | docs | docs-drift | `simple-gui.md:15` attributes the RAM auto-stop to *"2 GB boards at UHD/4K"*. PI-016 measured the dev unit at **4048 MB** with never less than ~2970 MB available at the sensor's true peak, and ADR-001 was corrected accordingly. The claim may still hold for genuine 2 GB boards — which remain unmeasured — but it now rests on the same unverified premise the ADR had to retract | `docs/simple-gui.md:15`, `PI-RESULTS-2026-08-24.md` (PI-016), `decisions/ADR-001-gui-harmonization.md` |
| F-307 | high | confirmed | cinemate | correctness | **Correction to F-291, found while fixing it: two separate, compounding bugs, not one.** The "Restart Cinemate" button in the settings editor never called the backend at all — `runBootSequence()` only ever played a canned client-side animation; clicking it made no HTTP request whatsoever. Separately, `restart_cinemate()`'s `os.execl()` re-exec was itself broken: the Flask/SocketIO listening socket survives `exec()` (it is not close-on-exec), confirmed with a standalone repro against real hardware — the re-exec'd process failed to rebind its port and the web GUI stayed dead. So even a correctly-wired button would have left the operator locked out after a "successful" restart. Both fixed in B11.4 (button now POSTs `restart cinemate` to `/api/v1/cmd`; `restart_cinemate()` now asks systemd via `systemd-run` instead of re-execing in place — a direct `systemctl restart` was tried first and also confirmed broken, killed by the unit's own `KillMode=control-group` stop signal before the restart job queues). A third, pre-existing, separately-documented systemd race was also hit during verification and left open: `cinemate-console-handoff.sh`'s `ExecStopPost` starts `getty@tty1.service`, which the unit's `Conflicts=getty@tty1.service` can race against an in-flight restart's start phase — reproduced 3/4 times in this session (worse than that script's own comment's "occasionally"), landing the unit `inactive` instead of running. Not fixed here; that script already names and rejects four prior attempts | `src/module/app/templates/settings_editor.html` (`restartBtn` handler), `cinepi_controller.py:2400` (`restart_cinemate`), `cinemate-install.sh` (sudoers), `services/cinemate-autostart/cinemate-console-handoff.sh:52-64`; B11.4 |
| F-308 | high | confirmed | install | install-drift | **Correction to F-289, found while fixing it: on the real test device, avahi-daemon was already installed and active.** `systemctl status avahi-daemon` showed it running since boot; `apt-mark showmanual` listed it explicitly installed (not pulled in as a dependency); the image is `pi-gen` stage2 (Lite), dated 2026-06-18. `cinepi.local` resolved without any change made. F-289's missing-avahi mechanism did not reproduce on this hardware — current Raspberry Pi OS Lite images appear to ship it by default, matching the finding's own hedge ("only ever worked when the base image happened to ship it"). The installer hardening (explicit `avahi-daemon`+`libnss-mdns` install, idempotent `/etc/hosts` 127.0.1.1 fix) was still added in B11.2 as a defensive measure against a future or different base image, but is **not a confirmed fix for the original field report** — the operator's actual failure needs more information (client OS, network, which image) to root-cause | `cinemate-install.sh` (`configure_hostname_and_i2c`, `configure_mdns`, `configure_etc_hosts`); B11.2, verified against real hardware at 192.168.2.6 |
| F-309 | high | confirmed | cinemate | correctness | **The settings editor's `hardware_controls` and `quad_rotary_controller` edits never actually saved, regardless of what the operator changed.** `buildHardwareControlsState()`/`buildQuadRotaryState()` built fresh objects from the form on every save, but the save handler immediately overwrote both with `lastLoadedSettings`'s original values — a deliberate workaround for F-292's 3-button/2-switch ceiling, so the old fixed UI could not silently drop a real file's 4th button or its `rotary_encoders`. Found while rebuilding the GPIO panes for B11.5, which removes the override now that the UI can represent every binding losslessly | `src/module/app/templates/settings_editor.html` (save handler, `populateHardwareControlsAndQuadRotary`); B11.5 |
| F-310 | high | confirmed | cinemate | correctness | **`hardware_outputs.rec_out_pin`, `rec_tone.pin`, `rec_tone.duty_cycle`, and `pwm_pin` had no `[data-path]` element anywhere on the settings editor page, so `config_loader.py`'s `_apply_settings_defaults()` `setdefault()`'d them back to `[6, 21]`/`[]`/`50`/`19` on every single settings save — for any reason, regardless of what the operator was actually editing.** The tally-pin `<select>` populated on load but was never wired to the save payload at all. Found while rebuilding the GPIO OUT pane for B11.5, which fixes it by building `hardware_outputs.rec_out_pin`/`rec_tone.pin` fresh from the (now-complete) form and adding the missing `duty_cycle` field; `pwm_pin` (unused elsewhere in the codebase) is preserved from the last real load rather than exposed, since nothing reads it | `src/module/config_loader.py` (`_apply_settings_defaults`, `hardware_outputs` block), `src/module/app/templates/settings_editor.html` (old tally-pin select had no `data-path`); B11.5 |
| F-311 | medium | confirmed | cinemate | correctness | **`applyActionToSlot()` defaults any gesture whose configured action is `"None"` (or absent) to `method='rec'` ("start/stop recording") instead of representing "no action".** Pre-existing in `dev`, latent: the old fixed-slot UI never actually exercised this path for a real `"None"` press action — the multi-function button simply had no press slot in the HTML at all, so `actionFromSlot`'s own null-slot fallback returned `"None"` correctly, bypassing `applyActionToSlot()` entirely. A full save round-trip test built for B11.5 (Node+jsdom, not committed) hit it directly: a button whose `press_action` was `"None"` came back as `{"method":"rec"}` after one populate-then-save cycle. Fixed in B11.5 — a real "No action" option was added to the method picker; `applyActionToSlot()`/`renderActionSlot()` now default to `''` instead of `'rec'` | `src/module/app/templates/settings_editor.html` (`applyActionToSlot`, `renderActionSlot`, `buildActionMethodSelect`); B11.5 |
| F-312 | high | confirmed | cinemate | dead-code | **The settings editor's single inline `<script>` throws a synchronous, uncaught `TypeError` on load, in every browser, on unmodified `dev` — right now.** `document.getElementById('liveIsoSelect').addEventListener(...)`: `liveIsoSelect` (and `liveShutterSelect`/`liveFpsSelect`/`liveWbSelect`/`liveRecBtn`/`liveTally`/`liveRecTime`) do not exist anywhere in the page. Dead code from the original settings-editor-ui mockup's fake "Live view" controls, left in place after they were replaced by a real embedded iframe (`live-embed-wrap`, whose own copy says *"nothing here is a mockup"*). **Every statement after this line in the script never runs.** Confirmed by loading the real template into a Node+jsdom harness built to verify B11.5's round-trip logic, which could not get past this line until the dead block was deleted — meaning on unmodified `dev`, in a real browser, right now: the save button, GPIO pin-select initialization/conflict-detection, the "+ Add gesture" dropdowns, and page search are all silently unwired. Fixed by deletion in B11.5 (and carried into B11.8, which touches the same file and needed the same fix to test its own JS) | `src/module/app/templates/settings_editor.html` (dead block, ~30 lines, removed); B11.5, B11.8 |
| F-313 | — | confirmed | docs | docs-drift | **B11.7's brief cites "ADR-001's constraint 2 and PI-009's result" for the web GUI's scale-vs-reflow question; that citation doesn't hold up.** ADR-001's C2/PI-009 is about DRM plane composition on the Pi's local HDMI output (`fbcon` vs `cinepi-raw`'s own preview contending for a plane) — unrelated to CSS layout strategy for a remote browser client. Neither document mentions scaling, reflow, breakpoints, or the hamburger menu anywhere; a full-text search of `ADR-001-gui-harmonization.md` for those terms returns nothing. The real precedent for "the HDMI GUI scales its regions" is `simple_gui.py`'s own layout table — a fixed 1920×1080-authored coordinate set, uniformly scaled per-draw by `disp_width/1920`, `disp_height/1080` — read directly for B11.7's fix instead. Also found in the same pass: the phone hamburger (F-296) only exists in `settings_editor.html`; `template.html` has no hamburger implementation at all, so there is nothing to characterize a failure of on that specific page | `decisions/ADR-001-gui-harmonization.md` (no scaling/reflow/hamburger content); `src/module/simple_gui.py` (`shrink_x`/`shrink_y` layout scaling); `src/module/app/templates/template.html` (no hamburger markup/JS); B11.7 |
