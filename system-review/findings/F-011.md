# F-011 — REFUTED: the dirty working tree described in KICKOFF §6.4 does not exist here

- **repo:** cinemate
- **severity:** — (withdrawn)
- **confidence:** refuted
- **category:** —
- **evidence:** `git status --short` (empty), `git rev-parse origin/dev`
- **needs-pi:** no
- **effort:** —

## What

KICKOFF §6.4 F-011 and §6.1 assert:

- cinemate `dev` has **8 uncommitted modified files** (settings-editor + recovery-console work)
- cinepi-raw local `dev` is **1 commit ahead** of `origin/dev`
- §11 step 3 therefore instructs S01 to **ask the operator** how to handle the dirty tree
  before cutting branches

None of this reproduces in the S01 environment.

## Observed instead

| Claim | Observed |
|---|---|
| 8 modified files | `git status --short` returns **empty**. Tree is clean. |
| `dev` @ `02b5a39` | Correct — and `origin/dev` is also `02b5a39`, fully pushed. |
| in-flight settings-editor work | Present **as commits**, not as edits: `8932ae2 feat(settings-editor): real RAW files pane (browse/download/delete)`, then `48d2f84` and `02b5a39` (docs cleanups). |
| cinepi-raw 1 commit ahead of origin | Not observable — no local cinepi-raw checkout existed. See Deviation D2 in `STATE.md`. |

The session container clones fresh from the remote, so uncommitted working-tree state
from the operator's own machine cannot be present by construction. The work KICKOFF
described as in-flight was committed and pushed before this review started.

## Consequence for the plan

**KICKOFF §11 step 3 is satisfied with nothing to ask.** There was no unilateral decision
to make: no stash, no commit, no discard. The operator's work was never at risk.

If the operator still has uncommitted changes on their **local** machine, that is
invisible from here and remains their call — but it does not block the review, because
the ledger branch is cut from `origin/dev`, which is a clean published state.

## Why this is recorded rather than deleted

Per KICKOFF §6: "If one is wrong, say so in the ledger — that is a useful result too."
The ID is burned so no future session reuses it, and the refutation is durable so a later
session does not re-read `git status` looking for phantom edits.
